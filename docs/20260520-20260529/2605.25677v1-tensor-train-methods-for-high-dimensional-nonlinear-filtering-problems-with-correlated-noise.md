---
title: Tensor train methods for high-dimensional nonlinear filtering problems with correlated noise
title_zh: 基于张量列方法的高维非线性滤波问题（含相关噪声）
authors: "Yuhua Meng, Stephen S. -T. Yau, Zhiwen Zhang"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25677v1"
tags: ["query:hsi-denoise"]
score: 8.0
evidence: 张量列用于带噪声的高维数据恢复
tldr: 针对相关噪声非线性滤波中DMZ方程无法转化为确定性PDE的难题，本文提出基于张量列（TT）的数值框架。通过空间离散化将SPDE转化为高维随机微分系统，利用TT近似压缩维数，并采用半隐式Milstein时间积分。理论分析表明空间误差受网格尺寸与TT精度控制，时间方向收敛借助含漂移观测的随机积分估计。数值实验在立方传感器问题上表现稳定准确，在多模态场景下优于粒子滤波和扩展卡尔曼滤波，有效捕获后验分布。该工作为高维相关噪声滤波提供了高效高精度求解方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25677v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1655, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25677v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1659, \"height\": 426, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25677v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1665, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25677v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1665, \"height\": 399, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25677v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 932, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25677v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1201, \"height\": 929, \"label\": \"Table\"}]"
motivation: 相关噪声使DMZ方程无法化简为确定性PDE，直接数值求解高维SPDE面临维数灾难。
method: 利用张量列（TT）压缩空间离散后的高维随机微分系统，结合半隐式Milstein时间积分，并建立收敛性分析。
result: 空间误差由网格尺寸与TT精度共同控制；时间收敛无需测度变换；立方传感器问题稳定准确，多模态场景优于粒子滤波和扩展卡尔曼滤波。
conclusion: 本文TT框架有效求解高维相关噪声滤波问题，在多模态环境中保持准确性并精确捕获后验分布。
---

## 摘要
含相关噪声的非线性滤波会导致Duncan-Mortensen-Zakai (DMZ)方程，其形式为随机偏微分方程(SPDE)。与独立噪声情况不同，相关性的存在使得经典的将DMZ方程转化为确定性偏微分方程的可逆变换无法应用，因此需要对SPDE进行直接数值处理。本文开发了一种基于张量列(TT)的框架，用于求解中高维含相关噪声的DMZ方程。空间离散化将SPDE转化为高维随机微分系统，并通过TT近似进行高效压缩。采用半隐式Milstein格式进行时间积分，以确保稳定性和精度。在适当的正则性假设下，我们建立了该方法的收敛性分析。特别地，空间误差由网格尺寸和预设的TT近似精度共同控制。在时间方向上，通过估计涉及漂移观测的随机积分来证明收敛性，而无需采用测度变换论证。数值实验表明，该方法在三次传感器问题中实现了稳定且准确的性能。在挑战性的多模态设置中，当粒子滤波和扩展卡尔曼滤波性能下降时，所提方法仍能保持精度并有效捕捉后验分布。

## Abstract
Nonlinear filtering with correlated noise leads to a Duncan-Mortensen-Zakai (DMZ) equation in the form of a stochastic partial differential equation (SPDE). Unlike the independent noise case, the presence of correlation prevents the classical invertible transformation that reduces the DMZ equation to a deterministic partial differential equation, requiring a direct numerical treatment of the SPDE. This paper develops a tensor train (TT) based framework for solving medium- to high-dimensional DMZ equations with correlated noise. Spatial discretization transforms the SPDE into a high-dimensional stochastic differential system, which is efficiently compressed using TT approximation. A semi-implicit Milstein scheme is employed for temporal integration to ensure stability and accuracy. Under suitable regularity assumptions, we establish a convergence analysis of the proposed method. In particular, the spatial error is controlled by both the mesh size and the prescribed TT approximation accuracy. In the temporal direction, the convergence is proved by estimating stochastic integrals involving drifted observations, without invoking a change-of-measure argument. Numerical experiments demonstrate that the proposed method achieves stable and accurate performance for cubic sensor problems. In challenging multi-modal settings, where particle filter and extended Kalman filter deteriorate, the proposed method maintains accuracy and effectively captures the posterior distribution.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在高维非线性滤波问题中，当信号与观测噪声相关时，经典的 Duncan-Mortensen-Zakai (DMZ) 方程无法通过可逆变换转化为确定性偏微分方程（PDE），必须直接求解一个随机偏微分方程（SPDE）。直接数值求解面临两个主要挑战：一是高维空间离散带来的“维数灾难”，二是相关噪声下 SPDE 的时间离散缺乏成熟的分析工具。
- **研究动机**：传统方法（如粒子滤波 PF、扩展卡尔曼滤波 EKF）在非线性、非高斯或多模态场景中性能严重下降，且 PF 在高维时计算成本急剧增加。因此，需要一种能够有效处理高维、非线性且相关噪声的滤波算法。
- **整体含义**：该工作将张量列（Tensor Train, TT）技术引入相关噪声滤波问题，提供了一种同时缓解维数灾难并提供严格收敛性保证的数值框架，为高维非线性滤波的实时求解开辟了新路径。

### 2. 论文提出的方法论

- **核心思想**：利用 TT 分解对空间离散后得到的高维随机微分系统进行低秩近似压缩，并采用半隐式 Milstein 格式进行时间积分，在保证稳定性的同时达到强一阶收敛。
- **关键技术细节**：
  - **空间离散**：假设扩散系数满足对角化假设（GGᵀ 和 ρρᵀ 为对角阵），在立方体域上使用中心差分和拉普拉斯算子的离散形式，将 SPDE 转化为高维 SDE：
    \[
    dU(t) = \mathcal{L}_0 U(t) dt + \sum_{k=1}^d \mathcal{L}_k U(t) dy_t^k
    \]
    其中 \(\mathcal{L}_0, \mathcal{L}_k\) 为离散化后的算子。
  - **TT 压缩**：将高维解张量 \(U(t)\) 和算子矩阵 \(\mathcal{L}_j\) 用 TT 格式表示，参数规模从 \(O(N^d)\) 降至 \(O(d N r^2)\)，其中 \(N\) 为每维网格点数，\(r\) 为 TT 秩。
  - **时间离散**：采用半隐式 Milstein 格式：
    \[
    \hat{U}_{n+1} = \left(I - \frac{\delta}{2}(\tilde{\Delta}_G + \tilde{\Delta}_\rho)\right)^{-1}\left( I - \delta(\tilde{C}_d + \tilde{M}_{(0)}) + \sum_{j} \tilde{L}_j I_n^{(j)} + \sum_{i,j} \tilde{L}_{(i,j)} I_n^{(i,j)} \right)\hat{U}_n
    \]
    其中隐式处理扩散项以增强稳定性，预计算逆算子（通过 Newton–Schulz 迭代在 TT 格式中完成）。
  - **收敛性分析**：
    - 空间误差：\(O((\Delta x)^4 + \varepsilon^2)\)（\(\varepsilon\) 为 TT 近似相对精度）。
    - 时间误差：通过直接估计含漂移观测的随机多重积分，在不借助测度变换的情况下证明强一阶收敛。
- **算法流程**（Algorithm 1）：
  1. **离线阶段**：计算 TT 格式下的所有算子逼近，预计算隐式部分的逆。
  2. **在线阶段**：每步时间步由观测增量驱动，计算随机积分近似值，执行显式线性组合后应用隐式逆算子更新解。

### 3. 实验设计

- **数据集/场景**：
  - **立方传感器问题 (Cubic Sensor)**：信号-观测模型为 \(dx = (A_d x + \sin(1.5x))dt + \frac{1}{\sqrt{5}} dv + \sqrt{\frac{2}{5}} dw\)，\(dy^{(k)} = (x^{(k)})^3 dt + dw^{(k)}\)。维度取 \(d=5,7,9\)，截断域 \([-2.2, 2.2]^d\)，网格每维 \(N=8\)，时间 \(T=20s\)，步长 \(\delta=0.01s\)。
  - **多模态例子 (Multi-mode)**：4 维系统，漂移含正弦函数，观测为二次加余弦偶函数，导致后验边缘密度呈双峰对称。域 \([-4.5,4.5]^4\)，\(N=25\)，\(T=20s\)，\(\delta=0.01s\)。
- **Benchmark**：以真实状态轨迹为参考，采用 RMSE 衡量估计精度，同时比较在线 CPU 时间。
- **对比方法**：粒子滤波（PF，不同粒子数）、扩展卡尔曼滤波（EKF）。

### 4. 资源与算力

- **明确说明**：论文未提及使用的 GPU 型号、训练时长或集群信息。所有计时实验均报告 CPU 时间（单位：秒），如 Table 2 中 7D 问题 TT 方法平均在线耗时 21.19s，PF 需 128.88s（11k 粒子）。因此，实验在普通 CPU 上完成，无需特殊 GPU 资源。
- **未说明部分**：模型离线阶段（如 TT 算子构建与逆矩阵计算）的具体耗时未被量化。

### 5. 实验数量与充分性

- **实验数量**：
  - 立方传感器问题：每个维度（5D、7D、9D）进行 **100 次独立随机试验**，统计平均 RMSE 和在线 CPU 时间。对比 PF 在每种维度下使用 2k~11k 粒子数。
  - 多模态问题：单个试验可视化，展示后验密度热图，并与 PF（5k 粒子）和 EKF 的轨迹对比。
- **充分性评价**：
  - **充分**：多次独立试验（100 次）有效降低了单次随机性的影响，统计结果可靠。
  - **公平**：PF 的粒子数设置合理（从低到高），并注意比较了计算时间；EKF 因频繁发散被排除。TT 方法在所有对比中均展示出更好的 RMSE 和更短/可比的在线时间。
  - **欠缺**：缺少更高维度（如 10D 以上）的系统测试；未进行 TT 秩对精度影响的消融实验；缺少对时间步长 \(\delta\) 的收敛性验证（如不同 \(\delta\) 下的 RMSE）。

### 6. 论文的主要结论与发现

- **方法有效**：TT 方法在立方传感器问题中，对于 \(d=5,7,9\) 均实现了比 PF 更低的 RMSE，且在线计算时间更短（或相当）。例如 7D 时 TT 的 RMSE=0.4472，时间=21.19s；PF 使用 11k 粒子 RMSE=0.4920，时间=128.88s。
- **多模态场景优势**：TT 方法直接近似概率密度，能够正确恢复双峰对称的后验分布；而 PF 因随机抽样特性倾向于集中在某个模态，EKF 发散。
- **收敛性保证**：建立了空间、时间及 TT 误差的联合收敛分析，证明了半离散 TT 解以 \(O((\Delta x)^4 + \varepsilon^2 + \delta^2)\) 收敛到真解（在均方意义上）。
- **克服维数灾难**：TT 格式的复杂度随维度多项式增长（\(O(d N r^3)\)），使 7D～9D 问题在普通计算资源上即可实时求解。

### 7. 优点

- **方法创新**：首次将 TT 分解应用于相关噪声下 DMZ 方程的数值求解，填补了中高维相关噪声滤波的空白。
- **理论贡献**：时间收敛的直接证明避免了传统测度变换的积分性损失，推导更简洁；空间误差分析同时考虑了网格离散和 TT 逼近误差，实用性强。
- **数值性能**：在强非线性（立方观测）和多模态场景下，TT 方法显著优于 PF 和 EKF，尤其对概率密度的直接表示使其天然适合多峰分布。
- **算法高效**：离线/在线分离结构，逆矩阵可预计算且具有低 TT 秩（Table 1 显示秩随维度增长缓慢），在线步仅需 \(O(dN^2 r^4)\) 运算。

### 8. 不足与局限

- **实验覆盖不足**：仅测试了结构化模型（对角扩散、简单域），未涉及强各向异性、不规则域或更高维度（如 20D）。TT 秩对复杂问题的适应性未充分考察。
- **假设条件较强**：理论分析要求扩散系数为对角阵（仅简化为 Assumption 3.1），且函数 \(f,h,\rho,G\) 需满足高阶光滑性和有界性，实际工程问题可能不严格满足。
- **数值实现近似**：时间离散中交叉项 \(I_{(i,j)}\) (\(i \neq j\)) 被近似为 \(\frac{1}{2} I_{(i)} I_{(j)}\)，可能将强收敛阶降至 0.5（论文 Remark 5.8 已指出）。文章虽采用 Milstein 格式，但实际收敛阶可能低于理论证明。
- **未讨论稳定性限制**：时间步长需满足 \(\delta < 1/C_h^2\) 等条件，对高维或快速变化观测可能带来限制。
- **缺乏消融研究**：未分析 TT 秩对精度-效率 trade-off 的影响，也无与其他降维方法（如 POD、稀疏网格）的定量比较。

（完）
