---
title: "Implicit Regularization in Perturbed Deep Matrix Factorization: Spectral Conditions and Stability"
title_zh: 扰动深度矩阵分解中的隐式正则化：谱条件与稳定性
authors: "Jingzhe Wang, Hung-Hsu Chou"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28613v1"
tags: ["query:hsi-denoise"]
score: 8.0
evidence: 噪声扰动下矩阵分解的低秩隐式正则化
tldr: 本文研究噪声环境下深度矩阵分解中隐式低秩正则化的稳定性。首先推导无噪声情况下梯度下降出现低秩阶段的充分谱条件，表明目标谱、初始化和步长共同决定低秩区间。接着分析扰动后的梯度下降动力学，证明收敛性并量化扰动对迭代复杂度和特征值恢复的影响。最后表明低秩阶段在扰动下持续存在，且依赖关系显式。数值实验验证了理论。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28613v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1294, \"height\": 530, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28613v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1293, \"height\": 530, \"label\": \"Figure\"}]"
motivation: 探究噪声矩阵扰动下，深度矩阵分解的隐式低秩正则化是否稳定及其条件。
method: 推导无噪声时梯度下降出现低秩阶段的谱条件，并分析扰动下的动力学与收敛性。
result: 低秩阶段在扰动下持续存在，迭代复杂度和特征值恢复受扰动大小显式影响。
conclusion: 揭示了谱条件、初始化和步长对低秩隐式正则化的联合作用，量化了噪声影响。
---

## 摘要
本文研究了扰动深度矩阵分解中低秩隐式正则化的稳定性，其中目标矩阵被噪声矩阵污染。我们首先推导了充分谱条件，使得在无噪声设置下梯度下降表现出低秩阶段。这些条件展示了目标谱、初始化和步长如何共同决定非空低秩区间的存在性。然后我们分析了受扰动的梯度下降动力学，证明了收敛保证并量化了扰动如何影响迭代复杂度和特征值恢复。最后，我们证明了低秩阶段在扰动下持续存在，且与扰动大小有显式依赖关系。数值实验支持了理论发现。

## Abstract
This paper studies the stability of low-rank implicit regularization in perturbed deep matrix factorization, where the target matrix is corrupted by a noise matrix. We first derive sufficient spectral conditions under which gradient descent exhibits a low-rank phase in the noiseless setting. These conditions show how the target spectrum, initialization, and step size jointly determine the existence of a nonempty low-rank interval. We then analyze the perturbed gradient descent dynamics, proving convergence guarantees and quantifying how the perturbation affects iteration complexity and eigenvalue recovery. Finally, we show that the low-rank phase persists under perturbation, with explicit dependence on the perturbation size. Numerical experiments support the theoretical findings.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究对象**：深度矩阵分解（Deep Matrix Factorization, DMF），即用多层线性神经网络（无激活函数）拟合一个目标矩阵。该问题等价于非凸优化问题：$\min_{W_1,\dots,W_N} \|W_N\cdots W_1 - \widehat{W}\|_F^2$。
- **核心问题**：在深度矩阵分解中，梯度下降算法会天然地偏好低秩解（隐式正则化），但现有理论主要分析无噪声情形。本文研究当目标矩阵被噪声污染时（$\widetilde{W} = \widehat{W} + E$），这种隐式低秩正则化是否仍然稳定存在？噪声如何影响迭代过程、特征值恢复以及低秩区间？
- **研究动机**：
  - 实际应用中目标矩阵常受测量噪声或隐私保护噪声干扰（如差分隐私矩阵近似）。
  - 需要量化扰动对隐式正则化行为的影响，以保证算法在噪声环境下的可解释性和可靠性。
- **整体含义**：提供一套关于谱条件、初始化和步长如何联合决定低秩现象的充分条件，并证明低秩阶段在适当扰动下依然存在，且误差显式依赖于扰动大小。

## 2. 方法论：核心思想与关键技术细节

### 核心思想
- 将梯度下降动力学在特征值层面解耦（利用对称矩阵的谱分解），每个特征值遵循一维标量迭代公式：$d(k+1) = d(k) - \eta\, d(k)^{N-1}(d(k)^N - \widetilde{\lambda})$。
- 无噪声情形下，识别出低秩区间 $[T_0, T_1]$：在此区间内，迭代矩阵的有效秩接近最佳秩-$L$ 逼近的有效秩。
- 噪声情形下，通过矩阵摄动理论（Weyl不等式、Davis-Kahan定理）将扰动矩阵的影响转化为特征值和特征向量的偏差，进而分析动力学变化。

### 关键技术细节
1. **无噪声情形的谱条件**（Lemma 3.1-3.5, Theorem 3.6）：
   - 假设 $N=2$，推导出 $T_0$ 和 $T_1$ 的显式表达式。
   - 给出非空低秩区间的充分条件：$T_1 - T_0 > 0$，这需要谱间隙足够大、步长足够小、初始化适当。
2. **扰动下的收敛性分析**（Theorem 4.3, 4.4）：
   - 证明梯度下降在扰动后仍收敛到 $\widehat{W}$ 的正部 $V\Sigma_+ V^\top$。
   - 量化了迭代复杂度上界（$T \le \widetilde{T} \le T^{Id}_N$）和下界。
   - 给出了特征值恢复误差的显式上界：$|E_{ii}(k)| \le \left(\frac{4\sqrt{2}M^N}{\delta_s}+1\right)\|E\| + \widetilde{\epsilon}N \widetilde{\lambda}_i^{1-1/N}$ 等。
3. **隐式正则化的稳定性**（Theorem 5.1-5.3）：
   - 主要结论：对 $k \in [\widetilde{T}_0, \widetilde{T}_1]$，有 $|r(\widehat{W}_L) - r(W(k))| \le \frac{2L}{\lambda_1}\|E\| + 3\epsilon\, r(\widetilde{W}^+_L)$。
   - 量化了噪声对低秩区间端点的偏移（Theorem 5.2）：$\widetilde{T}_1 - T_1, \widetilde{T}_0 - T_0$ 均被 $\|E\|$ 线性上界控制。
   - 给出了低秩逼近误差的Frobenius范数上界（Theorem 5.3）。

### 算法流程（文字说明）
1. **初始化**：所有权重矩阵初始化为 $\alpha I$（$\alpha>0$ 小常数）。
2. **梯度下降迭代**：对于每个 $j=1,\dots,N$，更新 $W_j(k+1) = W_j(k) - \eta \nabla_{W_j} \widetilde{L}(W(k))$。
3. **特征分解**：由于对称性和相同初始化，所有 $W_j$ 保持相同且为对角矩阵，可对角化为标量迭代。
4. **区间判断**：根据目标矩阵的谱、$\alpha$、$\eta$ 计算 $T_0, T_1$（或 $\widetilde{T}_0, \widetilde{T}_1$），在该区间内有效秩接近目标秩-$L$ 的有效秩。

## 3. 实验设计

- **使用场景**：合成矩阵，设置 $n=20$，目标矩阵为对称正定矩阵，指定前三个特征值（如 $\lambda_1=10, \lambda_2=5, \lambda_3=1$），其余为小正数。噪声矩阵 $E$ 为对称高斯矩阵，$E_{ij} \sim N(0, \sigma^2)$，控制 $\sigma = c/\sqrt{n}$ 使 $\|E\|$ 有界。
- **无标准 benchmark**：本文是理论驱动，没有与现有方法对比（如其他正则化方法或优化算法）。
- **对比条件**：
  - 变化步长 $\eta$ 观察低秩区间出现与否（图1a）。
  - 变化前三个特征值观察谱间隙的影响（图1b）。
  - 变化噪声水平 $c=0.5,1.0$ 与无噪声对比（图2a,2b）。
- **无对比方法**：仅与理论预测对比。

## 4. 资源与算力

- **未提及任何 GPU 型号、数量或训练时长**。实验为 MATLAB/Python 在小规模矩阵（$n=20$）上的模拟，计算量极低，普通 CPU 即可完成。

## 5. 实验数量与充分性

- **实验组数**：共3组主要实验（图1a,1b,2a,2b），每组内包含不同参数（步长/谱/噪声水平）的曲线。
- **充分性评价**：
  - 实验数量有限，但作为理论论文，实验主要用于验证定理的定性预测（如低秩区间存在性、噪声导致的偏移和误差增长），目的是支持而非全面验证。
  - 实验设计针对性强：图1a验证步长影响，图1b验证谱间隙影响，图2验证噪声影响。
  - **客观性**：参数选择在理论设定范围内，结果与理论一致。但缺乏不同网络深度 $N>2$ 或非对称矩阵的验证，也未在真实数据上测试。因此充分性中等。

## 6. 主要结论与发现

1. **无噪声情形**：推导出确保低秩区间非空且显式的谱条件（Theorem 3.6），即谱间隙必须足够大，步长足够小，初始化足够小。
2. **噪声情形下收敛性**：梯度下降仍收敛到 $\widehat{W}$ 的正部，迭代复杂度受噪声影响可量化（Theorem 4.3）。
3. **特征值恢复**：恢复误差由噪声范数 $\|E\|$ 线性主导，加上与精度 $\widetilde{\epsilon}$ 相关的可控项（Theorem 4.4）。
4. **隐式正则化稳定性**：低秩阶段持续存在，有效秩接近无噪声目标的最佳秩-$L$ 逼近的有效秩，误差项显式包含噪声贡献（Theorem 5.1）。
5. **区间偏移**：噪声缩短低秩区间（$\widetilde{T}_1$ 更早结束），偏移量被 $\|E\|$ 线性上界控制（Theorem 5.2）。
6. **逼近误差**：Frobenius范数下的低秩逼近误差也受噪声线性控制（Theorem 5.3）。

## 7. 优点

- **理论严谨**：给出了从谱条件到稳定性结果的一系列完整定理，证明步骤清晰，依赖矩阵摄动和标量动力学的经典工具。
- **显式量化**：几乎所有结果都给出了关于步长、初始化、噪声大小的显式界，便于实际指导超参数选择。
- **填补空白**：首次严格分析扰动深度矩阵分解中隐式正则化的稳定性，对差分隐私等应用有理论意义。
- **实验直观**：图像直观展示了低秩阶段的出现与消失，验证了理论预测。

## 8. 不足与局限

- **假设较强**：
  - 要求目标矩阵对称正定，噪声对称（实际应用可能不对称）。
  - 要求相同初始化 $W_j(0)=\alpha I$，且所有 $W_j$ 保持相同（非相同初始化情况仅提及但未分析）。
  - 步长范围受限（依赖于最大特征值等全局信息）。
- **实验覆盖有限**：
  - 仅测试 $N=2$，未验证更深网络 $N>2$ 的情况。
  - 未使用真实数据（如图像、推荐系统数据），仅用合成高斯噪声。
  - 未与现有隐式正则化方法（如 explicit regularization）对比。
- **应用限制**：理论主要关注有效秩的逼近，而非矩阵本身的精确恢复；且误差界可能较宽松（包含多个常数如 $\delta_s, c_N, K_\epsilon$），实际指导性有限。
- **缺少消融实验**：未单独验证每个假设（如不同的初始化 $\alpha$、不同的谱分布）的影响，也未分析噪声非高斯情况。

（完）
