Title: Adaptive, Matrix-Free Low-Rank Approximation

URL Source: https://arxiv.org/pdf/2607.06758v1

Published Time: Thu, 09 Jul 2026 00:09:15 GMT

Number of Pages: 25

Markdown Content:
ADAPTIVE, MATRIX-FREE LOW-RANK APPROXIMATION 

ARNEL I. SMITH ∗, ELLY DO † , AND CHAO CHEN ‡

Abstract. We study fixed-tolerance low-rank approximation in the matrix-free setting, where a matrix or linear operator A is accessible only through matrix-vector products and its rank must be determined adaptively to meet a prescribed error tolerance. We introduce a family of adaptive, matrix-free randomized QB algorithms. A randomized error indicator estimates the residual norm— in either the Frobenius or the spectral norm—directly from a random sketch, remaining accurate down to machine precision. A matrix-free rank-pruning step decouples the computational block size from the final rank, so that large, BLAS-3-friendly blocks can be used without over-estimating the rank, and an adjoint-free variant returns the orthonormal basis using only the forward operator. Across test matrices with diverse singular-value decays, the proposed methods attain ranks close to the truncated-SVD optimum while meeting the prescribed tolerance with high probability. 

Key words. low-rank approximation, matrix-free algorithm, adaptive rank determination, adjoint-free range finder, fixed-precision problem, randomized norm estimation 

AMS subject classifications. 65F55, 68W20, 15A23, 65F30, 15A18 

1. Introduction. We consider the fixed-tolerance low-rank approximation 

problem. Given a matrix A ∈ Rm×n and a target tolerance ε > 0, we seek to compute matrices Q ∈ Rm×k and B ∈ Rk×n such that (1.1) ∥A − QB ∥ ≤ τ, 

where the rank k ≪ min( m, n ) is a priori unknown and must be determined dynam-ically. In (1.1), the tolerance τ is a user-prescribed parameter, specified either as an absolute threshold τ = ε or a relative threshold τ = ε ∥A∥. We focus on the Frobenius norm ∥ · ∥ F and the spectral norm ∥ · ∥ 2. As is typical of randomized methods, the algorithms we develop satisfy (1.1) with high probability and, in the spectral norm, up to a modest constant (Theorem 4.3). We operate in the matrix-free setting : A is accessible only through matrix-vector products (matvecs)—the forward and adjoint actions x 7 → Ax and y 7 → A⊤y—while its individual entries are not. This setting is ubiquitous in computational science. In Bayesian inverse problems and uncertainty quantification, for instance, the Hessian of the log-posterior is defined implicitly, yet a low-rank approximation of it accelerates downstream optimization and sampling [4, 12]. There, a single matvec may require a full forward and adjoint PDE solve, so the dominant cost is the number of operator applications, not the internal dense linear algebra. Our goal is therefore to solve 

(1.1) while minimizing the number of matvecs, and to do so accurately for 

ε down to machine precision. 

We focus on a specific structural form of (1.1), the QB approximation [33, 47], where Q has orthonormal columns; the choice B = Q⊤A then minimizes the residual in the Frobenius norm. QB is a building block for many matrix factorizations, de-coupling range identification (the range finder) from entry approximation (the projec-tion): examples include the truncated SVD [15], the interpolative decomposition [8], the higher-order SVD for tensor compression [9], and hierarchical matrix approxima-tions [21, 29]. Each relies on the fixed-tolerance QB primitive that the matrix-free methods of section 3 and section 4 provide. 

> ∗

Department of Mathematics, North Carolina State University, Raleigh, NC (aismith@ncsu.edu). 

> †

Department of Mathematics, North Carolina State University, Raleigh, NC (ntdo2@ncsu.edu). 

> ‡

Department of Mathematics, North Carolina State University, Raleigh, NC (cchen49@ncsu.edu). 1arXiv:2607.06758v1 [math.NA] 7 Jul 2026 2 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

1.1. Existing Work and Limitations. 

Classical deterministic methods. Direct factorization methods such as the singu-lar value decomposition (SVD) and rank-revealing QR (RRQR) [16, 20] offer excel-lent stability and deterministic error bounds, but are designed for explicitly stored matrices: they require entry-wise access and scale as O(min( m, n )mn ), so they are inapplicable in the matrix-free setting. Iterative Krylov subspace methods such as Lanczos and Arnoldi [41] use only matvecs to extract the leading singular triplets. Although effective for rapidly de-caying spectra, they are numerically unstable in finite precision—the Lanczos vectors lose orthogonality, requiring expensive re-orthogonalization [36]—and converge slowly for flat or clustered spectra, requiring many sequential matvecs. Cross and skeleton methods such as the adaptive cross approximation (ACA) [1, 2] build skeleton decompositions from a sublinear number of rows and columns. Al-though scalable for smooth kernels (e.g., boundary element methods), ACA must evaluate individual entries Aij at arbitrary positions; for implicit operators such as Hessians, a single entry costs a full matvec, negating this advantage. Its greedy pivot-ing is also prone to instability or premature convergence on matrices with zero blocks, noise, or localized features [24]. 

Modern randomized methods. Randomized numerical linear algebra now provides a mature toolkit for low-rank approximation [32, 38]. Randomized rank-revealing fac-torizations, such as randomized column-pivoted QR [31, 11], use random embeddings to select pivot columns efficiently, bypassing the communication overhead of classical pivoting. However, they still update the trailing matrix explicitly, so they cannot operate in a matrix-free setting. Fixed-rank matrix-free methods such as the randomized SVD (RSVD) [22], the Nystr¨ om method for positive semi-definite matrices [14], and the generalized Nystr¨ om scheme [35] are matrix-free and parallelizable, but fixed-rank : the user must supply the target rank a priori (which can itself be estimated by randomized numerical-rank estimation [34]). Pass-efficient variants further reduce the number of views (passes) over A [3], but they too require the rank in advance. In multiscale applications such as H-matrix construction [6, 44, 21, 13], where the numerical rank fluctuates across thousands of off-diagonal blocks, a fixed rank causes either memory over-allocation or loss of fidelity. To address adaptivity, Halko, Martinsson, and Tropp [22, Algorithm 4.2] intro-duced an adaptive range finder, hereafter randQB HMT , the state-of-the-art (SOTA) adaptive method in the spectral norm; it is based on a probabilistic error bound that is pessimistic in practice and over-estimates the rank. Yu, Gu, and Li [47] intro-duced randQB EI , the SOTA adaptive method in the Frobenius norm, which builds the range block by block and tracks the residual error dynamically. However, it es-timates the residual Frobenius norm by Gramian-type tracking ( AA ⊤), squaring the effective condition number and capping the attainable precision near √εmach (≈ 10 −8

in IEEE double precision). Because its indicator subtracts ∥B∥2F from ∥A∥2F, it also requires the Frobenius norm ∥A∥F to be computed explicitly from A and is thus not matrix-free. A related adaptive Frobenius-norm method, by Gorman et al. [17], attains com-parable accuracy but certifies the error by post-processing the random sketches from all iterations, making it substantially more expensive than the rank pruning intro-duced here (subsection 3.2). More recently, Liu and Yu [28] proposed adaptive, fixed-threshold randomized algorithms with error analysis in both the Frobenius and spec-tral norms; like the methods above, however, they operate on the explicit entries of 3

A and are not matrix-free. Adaptive randomized schemes have likewise been devel-oped for other factorizations and settings, including rank-adaptive CUR [40], adaptive low-rank approximation for image compression [45], and dynamical low-rank approx-imation [5]. In short, no existing method is simultaneously matrix-free, adaptive in rank, and accurate down to machine precision in both the Frobenius and spectral norms; closing this gap is the goal of the present work. 

1.2. Contributions. The primary contribution is a family of adaptive, matrix-free QB algorithms that solve the fixed-tolerance low-rank approximation problem down to machine precision ( εmach ) in both the Frobenius and spectral norms. They build on the blocked randomized QB iteration [33, 47], which constructs Q and B

incrementally in blocks of b columns per iteration. Our main developments are: 

• Randomized residual indicators: Rather than a single, generic indicator, we develop a family of randomized error indicators tailored to each method— the sketched residual norm in the Frobenius case and a look-ahead block norm in the spectral case, each with an adjoint-free counterpart. Every indicator is read off the block sketch already computed at each iteration, at negligible cost, and avoids the failure modes of prior adaptive schemes: the “precision wall” near √εmach of energy-subtraction (Gramian) indicators [47], and the rank over-estimation caused by the conservative probabilistic bound of [22], remaining accurate and tight down to machine precision. 

• Rank Pruning and the Block-Size Dilemma: Our most significant al-gorithmic innovation is a post-iteration rank pruning step. Large block sizes 

b are needed for matrix-matrix (BLAS-3) efficiency, but they over-estimate the rank when the tolerance is met mid-block. Our pruning decouples the block size from the final rank, preserving high performance while retaining a near-minimal rank. 

• Adjoint-Free Adaptivity: We propose adjoint-free variants that determine the rank without the adjoint A⊤. When the adjoint is expensive or unavail-able and only the basis Q is needed, these variants roughly halve the passes over the data ( B is not formed). We emphasize that the basic matrix-free and adjoint-free blocked QB frameworks have appeared in prior work; our contributions are the rank-pruning step, the matrix-free estimate of ∥A∥ for relative tolerances, and a thorough numerical study of the resulting methods. Table 1 summarizes the four resulting methods, organized by norm and by whether the adjoint operator is required. Table 1: The four proposed adaptive, matrix-free QB methods. The matrix-free ( MF )variants use both the forward and adjoint actions ( A and A⊤); the adjoint-free ( AF )variants use only the forward action A.Matrix-free ( MF ) Adjoint-free ( AF )Frobenius norm randQB MF Fro (Alg. 3.1) randQB AF Fro (Alg. 3.3) Spectral norm randQB MF Spec (Alg. 4.1) randQB AF Spec (Alg. 4.3) 

2. Preliminaries and Problem Formulation. 4 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

2.1. Notation. Throughout this work, matrices are denoted by bold uppercase letters (e.g., A, B), vectors by bold lowercase letters (e.g., x, y), and scalars by stan-dard lowercase or Greek letters (e.g., k, ε ). The singular value decomposition (SVD) of A is denoted as A = UΣV ⊤, with singular values σ1 ≥ σ2 ≥ · · · ≥ 0. We utilize two standard matrix norms to measure the approximation error: 

• The Frobenius norm, defined as ∥A∥F =

qP 

> i,j

|Aij |2 =

qP 

> j

σ2 

> j

.

• The spectral norm, defined as ∥A∥2 = σ1.Our pseudocode uses two MATLAB-style primitives: randn (n, b ) returns an n × b

matrix with independent standard Gaussian N (0 , 1) entries, and orth (Y) returns a matrix whose columns form an orthonormal basis for the range of Y ∈ Rn×b with 

b < n , computed in practice via a thin (unpivoted) QR factorization. 

Remark 2.1 (Complex matrices). For clarity, all results are stated for real matri-ces. Every algorithm and result carries over verbatim to complex matrices by replacing each matrix transpose with the conjugate transpose. 

2.2. The Standard Randomized QB Decomposition. The standard ran-domized algorithm for computing a low-rank QB approximation proceeds in two pri-mary phases: range finding and projection. The method comes with a priori error guarantees: the approximation error is within a polynomial factor of the optimal value 

σk+1 (A) with high probability [22]; see [39] for a unified error analysis in both the Frobenius and spectral norms. It does, however, require the target rank k to be fixed in advance. The randomized QB method goes as follows: 1. Range Finding: Draw a Gaussian random test matrix Ω ∈ Rn×(k+p) (Here, 

p is an oversampling parameter chosen typically as 5 or 10). Compute the sketch Y = AΩ and form an orthonormal basis for its range: Q = orth (Y). 2. Projection: Project the original matrix onto this basis to form the core matrix: B = Q⊤A.For adaptive, fixed-tolerance settings, the blocked QB method [33, 47] builds Q

and B incrementally in blocks of size b. At the i-th iteration, let Q(i−1) and B(i−1) 

be the accumulated bases. The algorithm sketches the residual operator Ei−1 =

A − Q(i−1) B(i−1) as Yi = Ei−1Ωi using a block of random vectors Ωi ∈ Rn×b. Once 

Qi = orth (Yi) is computed, the block Bi is formed via Bi = Q⊤ 

> i

A. This process is summarized in Algorithm 2.1 Notice that Algorithm 2.1 measures the error at each iteration via the (Frobenius or spectral) norm of the residual matrix Ei. Forming Ei explicitly violates the matrix-free constraint: it costs as many matvecs as A has rows or columns. 

2.3. Estimating the Frobenius Norm. We recall a classical result on random-ized Frobenius-norm estimation, due to Hutchinson [25], which underlies our residual tracking without forming matrix entries. 

Theorem 2.2 (Lemma 1 in [25]). Let A ∈ Rm×n and let Ωi ∈ Rn×b be a random Gaussian matrix whose entries are independent and identically distributed normal random variables with mean zero and variance 1. Then 

E

 1

b ∥AΩ i∥2F



= ∥A∥2F.

Theorem 2.2 establishes unbiasedness in expectation; concentration bounds [18] further guarantee that a single realization stays close to the true norm with high prob-ability. For an order-of-magnitude estimate of the residual error, a small block size 5

Algorithm 2.1 Adaptive blocked QB iteration (variant of [33, 47]) 

Input: Matrix A ∈ Rm×n, (absolute) tolerance ε, and block size b. 

Output: Matrix Q with orthonormal columns and matrix B such that ∥A−QB ∥ ≤ ε

with high probability.  

> 1:

E0 = A 

> 2:

for i = 1 , 2, . . . do  

> 3:

Ωi ← randn (n, b ) ▷ Gaussian random block  

> 4:

Yi ← Ei−1Ωi ▷ Sample range space of the residual matrix  

> 5:

Qi ← orth (Yi) ▷ Thin QR decomposition  

> 6:

Bi ← Q⊤ 

> i

A 

> 7:

Ei ← Ei−1 − QiBi ▷ Update residual matrix Ei = A − Pik=1 QkBk 

> 8:

if ∥Ei∥ ≤ ε then  

> 9:

break ▷ Target tolerance reached  

> 10:

end if  

> 11:

end for  

> 12:

Q ← [Q1 Q2 · · · Qi] 

> 13:

B ← [B⊤ 

> 1

B⊤ 

> 2

· · · B⊤ 

> i

]⊤

(b = 5 or 10) suffices, so convergence can be monitored with cheap, low-dimensional sketches. 

2.4. Estimating the Spectral Norm. For stopping criteria in the spectral norm, we must track the singular values of the residual (and of A) from sketched information alone. We review a bound relating the singular values of the projection 

B = Q⊤A to those of A in the QB framework of subsection 2.2. 

Theorem 2.3 (Theorem 9 in [42]). Let B = Q⊤A be the projection produced by the randomized QB method with a Gaussian sketch Ω ∈ Rn×(k+p), k + p ≤ min( m, n ).Then, for i = 1 , . . . , k ,

σi(A) ≥ σi(B) ≥ σi(A)/η i,

where each factor ηi ≥ 1 is bounded by a constant depending only on k, p, and a prescribed failure probability, with high probability [19, Theorem 5.8]. 

By Theorem 2.3, the spectral norm of the projection B is a reliable proxy for the dominant singular values of A: with i = 1, ∥A∥2 = σ1(A) ≤ η1σ1(B) = η1∥B∥2.Applying this with the residual Ei = A − Q(i)B(i) in place of A lets us monitor the spectral-norm error without ever forming the residual. A second, complementary tool is the notion of a subspace embedding [32]: a sketch that preserves norms on a subspace also preserves its singular values. The simplest such embedding is a (scaled) random Gaussian matrix [32], which is the sketch used throughout this work. 

Lemma 2.4 (Subspace embedding and spectral approximation). Let A ∈ Rm×n

and let S ∈ Rs×m be an ε-subspace embedding for the range of A: for some 0 < ε < 1,

(1 − ε) ∥Ax ∥2 ≤ ∥ SAx ∥2 ≤ (1 + ε) ∥Ax ∥2 for all x ∈ Rn.

Then ˜A := SA is an ε-spectral approximation of A: it preserves A⊤A to relative error ε,

(1 − ε)2 A⊤A ⪯ ˜A⊤ ˜A ⪯ (1 + ε)2 A⊤A,6 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

and in particular (1 − ε) σi(A) ≤ σi( ˜A) ≤ (1 + ε) σi(A) for every singular value. Proof. The embedding condition squared reads (1 − ε)2 x⊤A⊤Ax ≤ x⊤ ˜A⊤ ˜Ax ≤

(1 + ε)2 x⊤A⊤Ax for all x, which is the stated Loewner ordering. The singular-value bounds follow from the Courant–Fischer min–max characterization. 

3. Adaptive Matrix-free Algorithms in Frobenius norm. Suppose A ∈

Rm×n is accessible only through its forward and adjoint actions x 7 → Ax and y 7 →

A⊤y. Our objective is to compute Q ∈ Rm×k with orthonormal columns and B =

Q⊤A ∈ Rk×n such that 

∥A − QB ∥F ≤ τ, 

where the target rank k is not known in advance and must be determined dynamically. The choice B = Q⊤A minimizes the Frobenius-norm error for a given basis Q. The tolerance is specified either as an absolute threshold τ = ε or a relative threshold 

τ = ε ∥A∥F, for some ε > 0. Standard approaches, such as the blocked QB algorithm [33], explicitly form the residual matrix Ei at every iteration (see the residual update in Algorithm 2.1). Consequently, they cannot be applied in the matrix-free setting. In this section, we demonstrate how to adapt the blocked QB framework into a purely matrix-free algorithm by resolving the following key algorithmic challenges: 1. Residual Estimation (subsection 3.1): We introduce a randomized error indicator that efficiently estimates the residual norm ∥Ei∥F without forming 

Ei. This bypasses the need for explicit matrix entries, enabling an adaptive stopping criterion relying exclusively on matrix-vector products. 2. Block Size Selection (subsection 3.2): We analyze strategies for choosing the block size parameter to maximize computational efficiency while prevent-ing severe overestimation of the numerical rank. 3. Adjoint-Free Variant (subsection 3.3): We introduce a forward-only modification that eliminates the need for adjoint queries ( y 7 → A⊤y) when only the basis Q is required. 

3.1. Randomized Error Indicator and Matrix-Free Algorithm. Recall the challenge in the blocked QB method is that the stopping criterion requires the error ∥Ei∥F, where the residual matrix (3.1) Ei = A −

> i

X

> k=1

QkBk i = 1 , 2, . . . . 

To avoid accessing entries of Ei, we use Hutchinson’s trace (Frobenius norm) estima-tor. But recall that a sample matrix Yi = Ei−1 Ωi is already computed to build the basis at every iteration in the blocked QB algorithm. Here, Ei−1 is the residual from the previous steps, and Ωi is a newly drawn random test matrix. Throughout, we nor-malize the random blocks so that Ωi ∈ Rn×b has i.i.d. N (0 , 1/b ) entries—equivalently, 

Ωi = randn (n, b )/√b—which absorbs the 1 /√b factor that would otherwise appear in every norm estimate. Applying Theorem 2.2 to √b Ωi then dictates that 

E ∥Yi∥2F

 = E ∥Ei−1Ωi∥2F

 = ∥Ei−1∥2F.

Therefore, we propose using ∥Yi∥2F as an unbiased randomized estimator of the current residual norm (squared) ∥Ei−1∥2F (see Theorem 2.2). 7We use the norm of the sample matrix as a proxy for the true error: (3.2) ∥Yi∥F ≈ ∥ Ei−1∥F.

Importantly, this estimator requires only the product Ei−1Ωi and does not require explicit access to the entries of the residual. Furthermore, evaluating this estimator incurs negligible O(bm ) additional computational cost. We no longer need Ei−1 for evaluating the error, but we still need to compute 

Yi = Ei−1 Ωi. How do we do this without storing Ei−1? Assuming i − 1 iterations are complete, we use a simple associativity trick: 

Yi = Ei−1Ωi = A −

> i−1

X

> k=1

QkBk

!

Ωi(3.3) = AΩ i

| {z } 

> black-box Mat-Vec

−

> i−1

X

> k=1

Qk (BkΩi)

| {z }

> small matrix mult

.

Forming Yi this way completely eliminates the need to ever form or store the residual matrix Ei−1.The combination of the randomized error indicator and the implicit residual ex-pansion gives rise to a fully adaptive, matrix-free algorithm, summarized in Algo-rithm 3.1. A defining feature of this new framework is that the sample matrix Yi

fulfills a dual algorithmic role. First, in its traditional capacity, it serves as the subspace generator to construct the next orthonormal block Qi. Second, by simply evaluating its Frobenius norm ∥Yi∥F, it simultaneously provides an estimate of the current residual error. 

Algorithm 3.1 randQB MF Fro : Adaptive, matrix-free low-rank approximation in Frobenius norm 

Input: Matrix A ∈ Rm×n, (absolute) tolerance 1ε > 0, and block size b. 

Output: Matrix Q with orthonormal columns and matrix B such that ∥A−QB ∥F ≤

ε with high probability.  

> 1:

for i = 1 , 2, . . . do  

> 2:

Ωi ← randn (n, b )/√b ▷ normalized Gaussian block  

> 3:

Zi ← AΩ i ▷ A is accessed via matvec  

> 4:

Yi ← Zi − Pi−1 

> k=1

Qk (BkΩi) ▷ Implicit residual application via (3.3)  

> 5:

if ∥Yi∥F ≤ ε then  

> 6:

break ▷ Last computed blocks are Qi−1 and Bi−1 

> 7:

end if  

> 8:

Qi ← orth (Yi) ▷ Thin QR decomposition  

> 9:

Qi ← orth 



Qi − Pi−1 

> k=1

Qk

 Q⊤ 

> k

Qi



▷ Re-orthogonalization  

> 10:

Bi ← Q⊤ 

> i

A ▷ A is accessed via adjoint matvec  

> 11:

end for  

> 12:

s ← Prune Rank Fro (Bi−1, ∥Yi∥F, ε ) ▷ See subsection 3.2  

> 13:

Q ← [Q1 . . . Qi−2 Qs

> i−1

] ▷ Qs 

> i−1

contain the columns subset s in Qi−1 

> 14:

B ← [B⊤ 

> 1

· · · B⊤ 

> i−2

(Bs

> i−1

)⊤]⊤ ▷ Bs 

> i−1

contain the row subset s in Bi−18 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

We now highlight two practical considerations for the robust and efficient imple-mentation of Algorithm 3.1. 1. Loss of Orthogonality: In exact arithmetic, the output matrix Q natu-rally maintains orthonormal columns. In practice, finite-precision arithmetic inevitably leads to a gradual loss of orthogonality. To counteract this, a re-orthogonalization step against the previously computed basis blocks is em-ployed after evaluating the new basis vectors. 2. Memory Allocation: The iterative algorithm computes the output matrices 

Q and B one block at a time. Dynamically reallocating memory for these matrices with increasing sizes at every iteration incurs a severe performance penalty. An optimized implementation should instead pre-allocate memory up to a maximum expected rank. If the iterative rank exceeds this estimate, the array sizes can be doubled to amortize the cost of reallocation. 

Remark 3.1 (Relative Tolerance). Thus far, we have considered an absolute tolerance, i.e., τ = ε. For a relative tolerance τ = ε ∥A∥F, we also require a matrix-free estimate of ∥A∥F. This makes the relative criterion more challenging than the absolute one: the algorithm must estimate ∥A∥F in addition to the residual norm, without explicit access to the entries of A. Let Zi = AΩ i as computed in Algorithm 3.1. Using the same probabilistic identity, we have 

E ∥Zi∥2F

 = ∥A∥2F.

Therefore, ∥Zi∥2F provides a randomized, matrix-free estimator of ∥A∥2F. Since a new block Zi is generated at every iteration, we can systematically reduce the variance of this estimate by aggregating the samples via a cumulative root-mean-square estimator: (3.4) Si ≡

vuut 1

i

> i

X

> k=1

∥Zk∥2F ≈ ∥ A∥F.

Because the quantities Zk are already computed during the basis generation phase, evaluating Si incurs only O(bm ) additional computational cost at every iteration. 

Remark 3.2 (Precision Limits). In the existing method randQB EI [47], the resid-ual norm is tracked by subtracting squared norms of the accumulated basis, i.e., 

∥Ei∥2F = ∥Ei−1∥2F − ∥ Bi∥2F. This approach is susceptible to catastrophic cancellation, limiting the achievable tolerance to O(√εmach ) (approximately 10 −8 in IEEE double precision). Because our newly proposed error indicator evaluates the residual norm directly via implicit application, it bypasses this cancellation issue and can reliably achieve precision on the order of O(εmach ). 

Remark 3.3 (Relation to prior work). The randomized Frobenius-norm indicator and blocked iteration underlying Algorithm 3.1 are not new in themselves: a random-ized (Hutchinson) Frobenius-norm estimate is used by Pearce et al. [37] to drive an adaptive interpolative decomposition, and the same matrix-free blocked QB frame-work underlies the hierarchically semiseparable construction of Gorman et al. [17], which does not report numerical results for the basic adaptive algorithm in isolation. Our main algorithmic contribution is the rank-pruning step of subsection 3.2, which decouples the block size from the final rank so that large, BLAS-3-friendly blocks can be used without over-estimating the rank; a secondary contribution is the matrix-free 

> 1See Remark 3.1 for the modification for relative tolerance.

9estimate of ∥A∥F for relative tolerances (Remark 3.1). We also provide a thorough nu-merical study (section 5) demonstrating the efficiency and machine-precision accuracy of the proposed methods. 

3.2. Block Size Selection and Rank Pruning. Modern cache-based comput-ing architectures are most efficient when computations can be organized as matrix-matrix products of sufficient size. For this reason, choosing a sufficiently large block size b in Algorithm 3.1 can substantially improve practical performance. The draw-back is that the output rank grows in multiples of b, meaning a large block size may overestimate the true numerical rank by as much as b − 1, leading to unnecessary storage and downstream computational costs. To mitigate this, we introduce a lightweight rank pruning procedure that prunes the final block produced by Algorithm 3.1. The pruning operates exclusively on the last computed block (the last b rows of B), and it has negligible computational overhead, just O(bn ) operations. The objective is to keep the computed low-rank approximation as compact as possible while still rigorously satisfying the prescribed tolerance. Suppose Algorithm 3.1 is applied to A ∈ Rm×n and the look-ahead test passes at iteration i ≥ 2, so the algorithm retains ˜Q = [ Q1 Q2 · · · Qi−1] and ˜B =[B⊤ 

> 1

B⊤ 

> 2

· · · B⊤

> i−1

]⊤. The state at termination reads: Previous step failed: ∥Ei−2∥F > τ where Ei−2 = A −

> i−2

X

> k=1

QkBk

Current step passed: ∥Ei−1∥F ≤ τ where Ei−1 = Ei−2 − Qi−1Bi−1.

Here, Qi−1 ∈ Rm×b and Bi−1 ∈ Rb×n are the final blocks added to the basis. (Tech-nically speaking, we don’t have the exact errors ∥Ei−2∥F and ∥Ei−1∥F but their randomized estimators.) Since the final update block Qi−1Bi−1 is simply a sum of b rank-1 outer products, our goal is to retain as few rank-1 outer products as possible to just cross the ε threshold, stripping away the excess rank. To formalize this approach, we partition the index set {1, . . . , b } into two disjoint subsets: 

{1, . . . , b } = s ∪ r, s ∩ r = ∅, |s| + |r| = b. 

Here, s denotes the indices to select and r denotes the indices to remove . Let Qs

> i−1

and Qr 

> i−1

contain the corresponding columns in Qi−1, and let Bs 

> i−1

and Br 

> i−1

contain the corresponding rows in Bi−1. It follows that the final block update can be split as 

Qi−1Bi−1 = Qs

> i−1

Bs 

> i−1

+Qr

> i−1

Br

> i−1

. Selecting the smallest necessary set s corresponds to solving min |s| subject to Ei−2 − Qs

> i−1

Bs  

> i−1F

≤ τ. (3.5) Once s is determined, the compact form of our low-rank approximation becomes Q =[Q1 · · · Qi−2 Qs

> i−1

] and B = [ B⊤ 

> 1

· · · B⊤ 

> i−2

(Bs

> i−1

)⊤]⊤, which satisfy ∥A−QB ∥F ≤ τ

with high probability. Solving (3.5) is equivalent to solving the following “dual” formulation: max |r| subject to Ei−1 + Qr

> i−1

Br  

> i−1F

≤ τ. (3.6) 10 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

This equivalence follows immediately from the fact that |r| = b − | s| and 

Ei−1 + Qr

> i−1

Br 

> i−1

= Ei−1 +  Qi−1Bi−1 − Qs

> i−1

Bs

> i−1



= ( Ei−1 + Qi−1Bi−1) − Qs

> i−1

Bs

> i−1

= Ei−2 − Qs

> i−1

Bs

> i−1

.

Computational Challenge: A naive approach for solving (3.6) is to start with 

∥Ei−1∥F and increase it by sequentially adding rank-1 outer products. However, forming the intermediate matrices explicitly is computationally expensive, and our method would lose its matrix-free advantage. Instead, we only need to track how the Frobenius norm changes when we add rank-1 outer products from the final block Qi−1Bi−1. Because the columns of Qi−1

are orthonormal, adding or subtracting a rank-1 component only changes the overall Frobenius norm by the magnitude of the row vector in Bi−1. This is stated as the following theorem: 

Theorem 3.4. Let Qj and Bj be the j-th blocks computed by Algorithm 3.1 (in exact arithmetic), and let Ej be the corresponding residual as defined in (3.1) . Then, for any index set r ⊂ { 1, . . . , b }, we have the following relation: 

Ej + Qr 

> j

Br 

> j
> 2F

= ∥Ej ∥2F + ∥Br 

> j

∥2F.

Proof. By the construction of the QB factorization, the residual Ej is orthogonal to the column space of Qj . Because Qr 

> j

is a submatrix of Qj with orthonormal columns, the columns of Ej are orthogonal to the columns of Qr 

> j

Br 

> j

. The identity then follows immediately from the matrix Pythagorean theorem. Consequently, applying Theorem 3.4 to the final block, the optimization problem in (3.6) reduces to selecting the largest subset of rows from Bi−1 whose squared Frobe-nius norms can be added to the squared residual norm without violating ∥Ei−1∥2F +

∥Br

> i−1

∥2F ≤ τ 2. To summarize, we do not need to form any explicit matrices. We only need to compute the row norms of matrix Bi−1!To retain as few rank-1 outer products as possible, we sort the row norms of matrix 

Bi−1 in ascending order; start with ∥Ei−1∥2F; and increase it by sequentially adding the row norms (squared) to just cross τ 2. This is summarized in Algorithm 3.2. Of course, we do not have the exact ∥Ei−1∥F (and ∥A∥F) but instead use their randomized estimators in (3.2) and (3.4). 

3.3. Adjoint-free Adaptive Range Finder. For applications where only the orthonormal basis Q is required and the projection B = Q⊤A is not explicitly needed, we present a variant of Algorithm 3.1 that requires only the forward operator A. By eliminating the need for the adjoint operator A⊤, this variant is both more broadly applicable and computationally faster. This task is commonly referred to as the range finder problem [22, 32]. In Algorithm 3.1, the matrix B is utilized in two specific locations: (i) the com-putation of the implicit residual Yi via the term BkΩi, and (ii) the rank-pruning step which requires the row norms of Bi−1. We address these two requirements without the adjoint operator as follows. 11 

Algorithm 3.2 Prune Rank Fro - Rank pruning in the Frobenius norm 

Input: Matrix B with b rows, error (indicator) α, and tolerance τ

Output: Index set s ⊆ { 1, 2, . . . , b } of columns/rows to select.  

> 1:

Compute B’s row norms {w1, w 2, . . . , w b}. 

> 2:

Sort {w1, w 2, . . . , w b} in ascending order such that wπ(1) ≤ wπ(2) ≤ · · · ≤ wπ(b)

for some permutation π. 

> 3:

r ← ∅ ▷ Initialize the set of redundant indices  

> 4:

for j = 1 , . . . , b do  

> 5:

α ←

q

α2 + w2 

> π(j)
> 6:

if α ≥ τ then  

> 7:

break ▷ Adding another row violates the tolerance  

> 8:

end if  

> 9:

r ← r ∪ { π(j)} ▷ Safely discard this index  

> 10:

end for  

> 11:

s ← { 1, 2, . . . , b } \ r ▷ Retain all non-discarded indices  

> 12:

return s

First, we observe that the term BkΩi (k = 1 , 2, . . . , i − 1) can be evaluated by shifting the parentheses to the right: (3.7) BkΩi = ( Q⊤ 

> k

A)Ωi = Q⊤ 

> k

(AΩ i) = Q⊤ 

> k

Zi

By computing Q⊤ 

> k

Zi, we obtain the necessary projection using only the already-computed sample matrix Zi.Second, the rank-pruning procedure typically requires the row norms of the final block Bi−1 to determine which columns of Qi−1 are redundant. In the adjoint-free case, we propose using the row norms of the sketch ˜Bi−1 = Bi−1Ωi as a proxy. According to Theorem 2.2, the squared row norms of ˜B are unbiased Hutchinson estimators of the squared row norms of Bi−1. Specifically, for the j-th row of the final block Bi−1, denoted by bj , the pruning algorithm relies on the estimator ˆ wj =

∥bj Ωi∥22. While we have established that E[ ˆ wj ] = ∥bj ∥22, the reliability of the pruning decision depends on the variance of this estimator, which is given by Var( ˆ wj ) =  

> 2
> b

∥bj ∥42. This variance decreases as the block size b increases, meaning that larger block sizes not only improve BLAS-3 efficiency but also enhance the precision of the adjoint-free rank refinement. In practice, even with moderate block sizes (e.g., 

b = 16), the probability of significantly misidentifying the “low-energy” rows is small. Furthermore, since the pruning step is only applied to the final block, any slight over-retention of columns due to estimator variance results only in a tiny increase in the final rank. We summarize our adjoint-free method in Algorithm 3.3. 

Remark 3.5 (Relation to an Existing Randomized Range Finder). Compared to the standard fixed-tolerance range finder [32, Algorithm 13], Algorithm 3.3 introduces two enhancements: (i) a relative tolerance can be implemented in a straightforward mechanism via Remark 3.1, and (ii) a matrix-free rank pruning step that produces a more compact basis with negligible overhead. That reference proposes the basic 

> 2See Remark 3.1 for the modification for relative tolerance.

12 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

Algorithm 3.3 randQB AF Fro : Adaptive, adjoint-free randomized range finder in Frobenius norm 

Input: Matrix A ∈ Rm×n, (absolute) tolerance 2ε > 0, and block size b.

Output: Matrix Q with orthonormal columns such that ∥A − QQ ⊤A∥F ≤ ε with high probability.  

> 1:

for i = 1 , 2, . . . do  

> 2:

Ωi ← randn (n, b )/√b ▷ normalized Gaussian block  

> 3:

Zi ← AΩ i ▷ Forward matvec only  

> 4:

Yi ← Zi − Pi−1 

> k=1

Qk

 Q⊤ 

> k

Zi

 ▷ Implicit application via (3.7)  

> 5:

if ∥Yi∥F ≤ ε then  

> 6:

break ▷ Last computed block is Qi−1 

> 7:

end if  

> 8:

Qi ← orth (Yi) ▷ Bi is not computed  

> 9:

Qi ← orth 



Qi − Pi−1 

> k=1

Qk

 Q⊤ 

> k

Qi



▷ Re-orthogonalization  

> 10:

end for  

> 11:

˜Bi−1 ← Q⊤

> i−1

Zi ▷ ˜Bi−1 is a sketch of Bi−1 via (3.7)  

> 12:

s ← Prune Rank Fro ( ˜Bi−1, ∥Yi∥F, ε ) 

> 13:

Q ← [Q1 . . . Qi−2 Qs

> i−1

] ▷ Qs 

> i−1

contain the column subset s in Qi−1

adjoint-free range finder but reports no numerical results for it; our numerical study (section 5) fills this gap. 

4. Adaptive Matrix-Free Algorithms in Spectral Norm. We now extend the matrix-free framework to the spectral norm (operator 2-norm). In this setting, the objective is to determine a target rank k dynamically such that the following approximation holds: (4.1) ∥A − QB ∥2 ≤ τ, 

where Q ∈ Rm×k has orthonormal columns and B = Q⊤A ∈ Rk×n. The tolerance τ

is a user-defined threshold, provided as either an absolute value τ = ε or a relative value τ = ε∥A∥2.As in the Frobenius-norm case, we seek to construct Q and B incrementally using only matrix-vector products with A and A⊤. However, the spectral norm presents a unique challenge: How to estimate the spectral norm of the residual matrix given a pair of factors Q and B. To address this, we resolve the following key algorithmic challenges: 1. Residual Estimation (subsection 4.1): We introduce a “look-ahead” ran-domized error indicator. By utilizing the fact that the QB framework natu-rally targets the dominant singular spectrum, the norm of the most recently computed block Bi provides an efficient estimate for the residual norm of the preceding approximation. 2. Block Size Selection (subsection 4.2): We analyze strategies for choosing the block size parameter to maximize computational efficiency while prevent-ing severe overestimation of the numerical rank. 3. Adjoint-Free Variant (subsection 4.3): We introduce a forward-only modification that eliminates the need for adjoint queries ( y 7 → A⊤y) when only the basis Q is required. 13 

4.1. Randomized Error Indicator and Matrix-Free Algorithm. The pri-mary difficulty in an adaptive spectral-norm QB algorithm is that the stopping crite-rion depends on ∥E∥2, where E = A − QB for a given pair of factors Q and B. We observe that at each iteration, the blocked QB method, i.e., Algorithm 2.1, generates a basis Qi whose span approximates the subspace spanned by dominant left singular vectors of the residual Ei−1. If the block size b is sufficiently large (e.g., b = 16), then, with probability almost 1, the error ∥Ei−1 − QiQ⊤ 

> i

Ei−1∥2 is close to the mini-mum error in rank-( b − p) approximation [22, Section 10], where p is the oversampling parameter (say p = 5 or p = 10). Consequently, the matrix 

Bi = Q⊤ 

> i

A = Q⊤ 

> i

Ei−1 +

> i−1

X

> k=1

QkBk

!

= Q⊤ 

> i

Ei−1

captures the action of the residual on its most significant subspace, where the first equality is by the definition of Bi, the second equality is by the definition of Ei−1 in (3.1), and the last equality holds by the orthogonality of the basis matrices Qi.Therefore, we use the spectral norm of the projected matrix as a proxy for the true error: 

∥Bi∥2 ≈ ∥ Ei−1∥2.

We summarize our adaptive, matrix-free algorithm in Algorithm 4.1. 

Remark 4.1 (Relative Tolerance). To adapt the stopping criterion for a relative tolerance τ = ε∥A∥2, we utilize the first computed block as an initial estimate of the operator norm: ∥B1∥2 ≈ ∥ A∥2. This allows the algorithm to remain entirely matrix-free even when the scale of A is unknown a priori .

Remark 4.2 (Computing the spectral norm). To evaluate ∥Bi∥2 efficiently, we avoid a full SVD. Since Bi ∈ Rb×n is a short-and-wide matrix, its spectral norm is the square root of the dominant eigenvalue of BiB⊤ 

> i

∈ Rb×b. Given that b is small, a few steps of the power method suffice to determine this norm to sufficient accuracy with a cost of only O(nb ) flops. 

4.2. Block Size Selection and Rank Pruning. The block-size dilemma de-scribed in subsection 3.2 applies equally here: a large b improves BLAS-3 throughput but lets the output rank k = i · b overshoot the minimal rank needed to meet the tolerance. We again prune the final retained block, but now in the spectral norm: instead of discarding rows by their norm, we truncate Bi−1 according to its singular value distribution. We prune the final block Bi−1 to the fewest singular directions needed: retain those with singular value σj (Bi−1) ≥ ε and discard the rest, strip-ping away the excess rank while keeping the truncation error within ε.The change of criterion is dictated by the norm. In the Frobenius case, the orthog-onality of Qi−1 makes the squared error additive over the rows of Bi−1 (Theorem 3.4), 

> 3See Remark 4.1 for the modification for relative tolerance.

14 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

Algorithm 4.1 randQB MF Spec : Adaptive, matrix-free low-rank approximation in spectral norm 

Input: Matrix A ∈ Rm×n, (absolute) tolerance 3ε > 0, and block size b. 

Output: Matrix Q with orthonormal columns and matrix B such that ∥A−QB ∥2 ≤

ε with high probability.  

> 1:

for i = 1 , 2, . . . do  

> 2:

Ωi ← randn (n, b )/√b ▷ normalized Gaussian block  

> 3:

Zi ← AΩ i ▷ A is accessed via matvec  

> 4:

Yi ← Zi − Pi−1 

> k=1

Qk (BkΩi) ▷ Implicit residual application via (3.3)  

> 5:

Qi ← orth (Yi) ▷ Thin QR decomposition  

> 6:

Qi ← orth 



Qi − Pi−1 

> k=1

Qk

 Q⊤ 

> k

Qi



▷ Re-orthogonalization  

> 7:

Bi ← Q⊤ 

> i

A ▷ A is accessed via adjoint matvec  

> 8:

if ∥Bi∥2 ≤ ε then  

> 9:

break ▷ tolerance met; the look-ahead block Bi is not retained  

> 10:

end if  

> 11:

end for  

> 12:

[Ur , Wr ] ← Prune Rank Spec (Bi−1, ε ) ▷ prune Bi−1, the last retained block  

> 13:

Q ← [Q1 . . . Qi−2 (Qi−1Ur )]  

> 14:

B ← B⊤ 

> 1

· · · B⊤ 

> i−2

W⊤

> r

⊤

so pruning reduces to dropping rows of small norm. The spectral norm is not additive in this way; the quantity that governs it is the singular-value spectrum of the block 

Qi−1Bi−1. Since Qi−1 has orthonormal columns, we compute the SVD of Bi−1 and, invoking the Eckart–Young theorem, retain exactly the leading r singular directions whose singular values reach the tolerance, discarding the trailing directions whose singular values—and hence whose contribution to the residual—fall below ε.Because the retained residual Ei−1 is orthogonal to the final basis block 

Qi−1, the block-level truncation error combines with ∥Ei−1∥2 in the Pythagorean sense rather than accumulating—so the pruned factors still meet the prescribed tolerance, up to the modest constant of Theorem 4.3. The procedure for rank pruning is summarized in Algorithm 4.2. Since Bi−1 has dimensions b×n, its SVD costs only O(nb 2) flops—negligible relative to the matvecs— yet it keeps the output rank close to the optimal numerical rank of the operator. The following theorem makes the tolerance guarantee precise. 

Theorem 4.3. Suppose randQB MF Spec (Algorithm 4.1 ) is applied to A ∈ Rm×n

with absolute tolerance ε and terminates at iteration i, i.e., the look-ahead block sat-isfies ∥Bi∥2 ≤ ε. Let ˜Q = [ Q1, . . . , Qi−1] and ˜B = [ B⊤ 

> 1

, . . . , B⊤

> i−1

]⊤ be the retained factors and Q, B the pruned factors returned by the algorithm. Then 

∥A − QB ∥2 ≤ pη2 + 1 ε, 

where η is the constant from Theorem 2.3 applied to the residual Ei−1 = A − ˜Q ˜B

defined in (3.1) .Proof. Note that [ Ur , Wr ] = Prune Rank Spec (Bi−1, ε ) with Wr = Σr V⊤ 

> r

,where r is the number of singular values of Bi−1 that are at least ε, i.e., σr (Bi−1) ≥15 

Algorithm 4.2 Prune Rank Spec : Rank pruning in spectral norm 

Input: Block B ∈ Rb×n and tolerance τ .

Output: Factors Ur and Wr = Σr V⊤ 

> r

such that ∥B − Ur Wr ∥2 ≤ τ . 

> 1:

[U, Σ, V] ← svd (B, ‘econ’)  

> 2:

Find indices s = {j : σj (B) ≥ τ } 

> 3:

Ur ← U(: , s), Wr ← Σ(s, s) V(: , s)⊤

▷ Wr formed by scaling the rows of V(: , s)⊤ 

> 4:

return Ur , Wr

ε > σ r+1 (Bi−1). Let N = Bi−1 − Ur Wr be the error incurred by truncating the last block. Since the pruning replaces Qi−1Bi−1 by Qi−1(Ur Wr ) and leaves the other blocks unchanged, we decompose the truncated residual as: 

A − QB = ( A − ˜Q ˜B) + Qi−1(Bi−1 − Ur Wr ) = Ei−1 + Qi−1N.

Since Q⊤

> i−1

Ei−1 = Q⊤

> i−1

A − Q⊤ 

> i−1

˜Q ˜B = Bi−1 − Bi−1 = 0, the cross terms vanish in (Ei−1 + Qi−1N)⊤(Ei−1 + Qi−1N) = E⊤

> i−1

Ei−1 + N⊤N, and the sub-additivity of the largest eigenvalue gives: 

∥A − QB ∥22 = ∥Ei−1 + Qi−1N∥22 ≤ ∥ Ei−1∥22 + ∥N∥22.(4.2) The look-ahead block satisfies Bi = Q⊤ 

> i

A = Q⊤ 

> i

Ei−1, so applying Theorem 2.3 to 

Ei−1 (with Bi as its projection) gives ∥Ei−1∥2 = σ1(Ei−1) ≤ η σ 1(Bi) = η ∥Bi∥2 ≤

ηε . By the Eckart–Young theorem, the truncation error is ∥N∥2 = σr+1 (Bi−1) < ε .Substituting these into (4.2) yields: 

∥A − QB ∥22 < η 2ε2 + ε2 = ( η2 + 1) ε2.

Taking the square root completes the proof. 

Remark 4.4. The constant η from Theorem 2.3 is pessimistic. When the block size is sufficiently large so that the range finder captures the dominant singular subspace of Ei−1 almost entirely, η is close to 1, and the attained error is very close to the prescribed ε. The numerical experiments in section 5 confirm that the observed error tracks ε closely, well within the bound of Theorem 4.3. 

4.3. Adjoint-free Adaptive Range Finder. As in the Frobenius-norm case (subsection 3.3), many applications require only the orthonormal basis Q and not the projection B = Q⊤A. We therefore present a variant of Algorithm 4.1 that accesses 

A through the forward operator alone, eliminating the adjoint queries y 7 → A⊤y.In Algorithm 4.1 the adjoint is used in two places: (i) the look-ahead indicator 

∥Bi∥2, where Bi = Q⊤ 

> i

A, and (ii) the rank-pruning step, which truncates Bi−1 by its singular values. We replace both with forward-only quantities. Both replacements rest on the same principle that Lemma 2.4 justifies: a matrix’s singular values are read from a Gaussian sketch of it. First, recall that Ei−1 = A − Pi−1 

> k=1

QkQ⊤ 

> k

A denotes the residual after the first 

i − 1 blocks. Its random sketch Ei−1Ωi can be assembled using only the forward action Zi = AΩ i:(4.3) Ei−1Ωi = A −

> i−1

X

> k=1

QkQ⊤ 

> k

A

!

Ωi = Zi −

> i−1

X

> k=1

Qk

 Q⊤ 

> k

Zi

 = Yi,16 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

where we have used ( Q⊤ 

> k

A)Ωi = Q⊤ 

> k

Zi to avoid forming Bk. As a result, the spectral norm of this sketch yields a look-ahead estimate of the residual, (4.4) ∥Yi∥2 = ∥Ei−1Ωi∥2 ≈ ∥ Ei−1∥2,

the spectral-norm analogue of the Frobenius indicator of subsection 3.3. It replaces 

∥Bi∥2 as a crude stopping criterion and is estimated by a few steps of power iteration applied directly to Yi.Second, the pruning step of Algorithm 4.2 truncates Bi−1 by its singular values, but Bi−1 = Q⊤

> i−1

A is never formed here. Exactly as in the Frobenius-norm case (3.7), we replace it with the forward sketch (4.5) ˜Bi−1 = Q⊤

> i−1

Zi = Q⊤

> i−1

A Ω i = Bi−1Ωi,

obtained from the current block’s sample Zi = AΩ i at no additional matvec cost. Since Ω⊤ 

> i

is, with high probability, a subspace embedding for the row space of Bi−1,Lemma 2.4 gives σj ( ˜Bi−1) ≈ σj (Bi−1). Computing the SVD ˜Bi−1 = UΣV ⊤ and retaining the r left singular vectors with σj ( ˜Bi−1) ≥ ε gives the pruned basis Q =[Q1 . . . Qi−2 (Qi−1Ur )], the adjoint-free counterpart of Algorithm 4.2. We summarize the procedure in Algorithm 4.3. A caveat is in order: guaranteeing a genuine ε-subspace embedding would require the block size b to grow like 1 /ε 2,far more than is practical, so the sketch delivers only a same-order estimate of the singular values rather than a highly accurate one. We emphasize, then, that (4.1) is to be understood in an approximate sense: unlike the Frobenius-norm methods of section 3, which certify the prescribed bound (with high probability), the spectral-norm algorithms do not guarantee (4.1) exactly, but instead keep the computed error 

∥A − QB ∥2 on the order of the tolerance τ .

Algorithm 4.3 randQB AF Spec : Adaptive, adjoint-free randomized range finder in spectral norm 

Input: Matrix A ∈ Rm×n, (absolute) tolerance 4ε > 0, and block size b.

Output: Matrix Q with orthonormal columns such that ∥A − QQ ⊤A∥2 ≲ ε with high probability.  

> 1:

for i = 1 , 2, . . . do  

> 2:

Ωi ← randn (n, b )/√b ▷ normalized Gaussian block  

> 3:

Zi ← AΩ i ▷ Forward matvec only  

> 4:

Yi ← Zi − Pi−1 

> k=1

Qk

 Q⊤ 

> k

Zi

 ▷ Adjoint-free residual sketch (4.3)  

> 5:

if ∥Yi∥2 ≤ ε then  

> 6:

break ▷ tolerance met; the look-ahead block is not retained, see (4.4)  

> 7:

end if  

> 8:

Qi ← orth (Yi) ▷ Thin QR; Bi is not computed  

> 9:

Qi ← orth 



Qi − Pi−1 

> k=1

Qk

 Q⊤ 

> k

Qi



▷ Re-orthogonalization  

> 10:

end for  

> 11:

˜Bi−1 ← Q⊤

> i−1

Zi ▷ forward sketch of Bi−1 via (4.5)  

> 12:

[Ur , ∼] ← Prune Rank Spec ( ˜Bi−1, ε ) ▷ prune sketch of the last retained block  

> 13:

Q ← [Q1 . . . Qi−2 (Qi−1Ur )]      

> 4For a relative tolerance τ=ε∥A∥2, the operator norm ∥A∥2is estimated from the forward sketches as described in Remark 4.5.

17 

Remark 4.5 (Relative tolerance). For a relative tolerance τ = ε∥A∥2, the adjoint-free method needs a matrix-free estimate of ∥A∥2. Each forward sketch 

Zi = AΩ i is itself a randomized sketch of A, so its spectral norm already sup-plies one: ∥Zi∥2 ≈ ∥ A∥2. Since the sketches Z1, . . . , Zi are all computed during the iteration, a sharper estimate is available at no extra matvec cost by concatenating them into Zi = [ Z1 · · · Zi] = A [Ω1 · · · Ωi], a sketch of A with i b columns, giving 

∥A∥2 ≈ 1

√i [Z1 · · · Zi] 2.

The estimate sharpens as i grows, and the factor 1 /√i rescales for the i b columns of the concatenated sketch, reducing to ∥Z1∥2 ≈ ∥ A∥2 at i = 1. 

5. Numerical Results. We evaluate the proposed matrix-free adaptive algo-rithms with two objectives: to show (1) that the adaptive stopping criteria accurately capture the target tolerance across a range of singular-value decay profiles, and (2) that the rank-pruning strategies yield a near-optimal output rank. All computations are performed in double precision. 

Reproducibility. The MATLAB implementations of the proposed algorithms, to-gether with the driver scripts that reproduce all numerical experiments and figures re-ported in this section, are publicly available at https://github.com/SMART-Algebra/ Adaptive-Matfree-Lowrank. 

5.1. Experimental Setup and Baselines. We compare the proposed algo-rithms against the following baselines: 1. Frobenius-norm baseline ( randQB EI [47]): the SOTA adaptive QB method. Its error estimate relies on a “top-down” subtraction of squared Frobenius norms, which is prone to catastrophic cancellation in finite precision and typ-ically limits its reliable tolerance to about O(√εmach ) (Remark 3.2). We show that our matrix-free variants remove this limitation, remaining stable down to machine precision. 2. Spectral-norm baseline ( randQB HMT [22, Algorithm 4.2]): the SOTA adaptive randomized range finder of Halko, Martinsson, and Tropp. It uses only the forward operator and returns only the orthonormal basis Q, so it is adjoint-free. Its a posteriori error estimate is provably reliable but pessimistic ([22, Remark 4.1]), so it typically overestimates the rank. 3. Theoretical optimum (truncated SVD, t-SVD): for any tolerance ε, the Eckart–Young theorem identifies the truncated SVD as the rank-k approxi-mation that minimizes the error in both the Frobenius and spectral norms. The SVD cannot be applied in the matrix-free setting without first form-ing the entire matrix, but we include it as a “gold standard” that measures the rank over-determination factor of our algorithms — how many extra di-mensions the proposed randomized methods need to match the error of the optimal (but infeasible) SVD. 

Test matrices. We test our methods on matrices exhibiting a range of decay pro-files, from rapid exponential decay (typical of smoothing operators) to slow algebraic decay (common in certain statistical datasets) to S-shape decay. Let A = UΣV ⊤,where U, V ∈ R1024 ×1024 are random Gaussian matrices and Σ ∈ R1024 ×1024 is diag-onal with entries defined below: 

• Matrix 1: σi = exp( −i/ 2) 1 ≤ i ≤ 1024 Exponential decay 

• Matrix 2: σi = 1 /i p 1 ≤ i ≤ 1024 Polynomial decay 18 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 

• Matrix 3: σi =



10 −6( i−1) /399 , 1 ≤ i ≤ 400 ,

10 a (i−400) −α+b, 401 ≤ i ≤ 1024 ,

S-shape decay where α = 10 −0.8 ≈ 0.158, and the constants a and b are fixed by requiring the tail to join the first piece continuously at σ401 = 10 −6 and to reach 

σ1024 = 5 εmach ; this gives a ≈ 14 and b ≈ − 20. The two exponents for Matrix 2 are chosen to stress-test the baselines, not to constrain our methods. For the Frobenius-norm experiments we take p = 4, so that part of the spectrum falls below √εmach and thereby exposes the precision wall of 

randQB EI . For the spectral-norm experiments we take p = 2, so that the numerical ranks are relatively large and all methods require many iterations to converge, which sharpens the contrast between the proposed methods and randQB HMT . These choices are designed to highlight the differences between the methods and are not limitations of the proposed algorithms. In all cases, the tolerance τ is specified as a relative threshold: τ = ε∥A∥, for a wide range of ε, where ∥·∥ denotes either the Frobenius or spectral norm, depending on the algorithm under test. The relative criterion is more demanding than an absolute one, as the algorithm must additionally estimate ∥A∥ on the fly; our methods do so matrix-free from the same sketches (Remark 3.1), whereas randQB EI computes ∥A∥F

explicitly from A and is thus not matrix-free. We use a block size of b = 32 throughout, except for Matrix 1, whose small numerical rank calls for b = 16 to keep the iteration counts comparable across ma-trices. For each method we report the achieved (exact) relative error in the relevant norm. For randQB EI and the matrix-free variants ( randQB MF Fro , randQB MF Spec ), it is ∥A − QB ∥/∥A∥, where Q and B are the computed pair. The adjoint-free vari-ants ( randQB AF Fro , randQB AF Spec ) and randQB HMT never form the projection 

B = Q⊤A, so their error is evaluated as ∥A − QQ ⊤A∥/∥A∥. For the t-SVD it is the optimal error of the best rank-k approximation. 

5.2. Comparison in the Frobenius Norm. We compare four methods: the truncated SVD (t-SVD; the optimal, but not matrix-free, baseline), randQB EI (SOTA, but not matrix-free), and the two proposed methods randQB MF Fro and randQB AF Fro .Figures 1 to 3 report the results for the exponential, polynomial, and S-shaped spectra (Matrices 1–3), respectively. Each figure shows the computed rank versus the pre-scribed relative tolerance ε (left) and the per-iteration error indicator of each method (right), together with a table of the computed rank k and the achieved (exact) relative error in the Frobenius norm (evaluated as described in subsection 5.1; tabulated at every other tolerance for brevity, while the panels use the full range). 

Accuracy and tolerance range. Across all three spectra, the proposed randQB MF Fro 

matches randQB EI wherever the latter is reliable, while remaining accurate over a far wider range of tolerances. Its achieved relative error tracks the prescribed tolerance almost perfectly, staying just at or below ε all the way down to machine precision. The adjoint-free variant randQB AF Fro is slightly less stable but still attains an achieved error of the same order as ε throughout. In contrast, randQB EI is reliable only for 

ε ≳ O(√εmach ); below that wall it breaks down (entries marked fail in the tables). 

Rank efficiency. The ranks produced by both proposed methods stay close to the optimal ranks of the t-SVD (typically within a handful of extra dimensions) so the matrix-free, randomized rank determination costs little. Unlike the t-SVD, however, the proposed methods access A only through matrix-vector products and never form a dense factorization, which makes them far cheaper for large operators. 19 10 -15 10 -10 10 -5 10 0    

> 010 20 30 40 50 60 70 020 40 60 80
> 10 -15
> 10 -10
> 10 -5
> 10 0

ϵ t-SVD randQB EI randQB MF Fro randQB AF Fro 

k rel. err k rel. err k rel. err k rel. err 1e-01 5 8.21e-02 6 8.79e-02 6 8.85e-02 7 5.59e-02 1e-03 14 9.12e-04 19 5.64e-04 18 7.86e-04 18 8.53e-04 1e-05 24 6.14e-06 27 7.86e-06 26 9.50e-06 26 8.44e-06 1e-07 33 6.83e-08 fail 36 6.63e-08 36 9.51e-08 1e-09 42 7.58e-10 fail 45 9.13e-10 45 8.28e-10 1e-11 51 8.42e-12 fail 56 4.84e-12 55 5.04e-12 1e-13 60 9.40e-14 fail 64 8.94e-14 64 8.13e-14 

Fig. 1: Matrix 1 (exponential, fast spectral decay). Below its precision wall, 

randQB EI ’s error indicator stops decreasing (right panel) and never meets the toler-ance; those entries are marked fail .10 -10 10 -5 10 0   

> 0200 400 600 800 1000 1200 010 20 30
> 10 -10
> 10 -5
> 10 0

ϵ t-SVD randQB EI randQB MF Fro randQB AF Fro 

k rel. err k rel. err k rel. err k rel. err 1e-01 1 6.37e-02 2 1.87e-02 1 8.11e-02 1 7.85e-02 1e-03 5 9.32e-04 7 6.43e-04 7 7.18e-04 7 9.38e-04 1e-05 20 9.64e-06 26 8.90e-06 28 9.76e-06 27 9.47e-06 1e-07 76 9.63e-08 fail 105 8.93e-08 105 9.40e-08 1e-09 282 9.95e-10 fail 383 9.98e-10 387 9.70e-10 1e-11 940 9.92e-12 fail 1024 1.51e-15 1024 1.49e-15 

Fig. 2: Matrix 2 (polynomial, slow spectral decay). Here the quantity ∥A∥2F − ∥ B∥2F

that randQB EI subtracts turns negative below the precision wall, so its indicator is undefined and can never reach the tolerance (marked fail ). 20 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 10 -15 10 -10 10 -5 10 0    

> 0200 400 600 800 1000 1200 010 20 30 40
> 10 -15
> 10 -10
> 10 -5
> 10 0

ϵ t-SVD randQB EI randQB MF Fro randQB AF Fro 

k rel. err k rel. err k rel. err k rel. err 1e-01 67 9.83e-02 93 9.91e-02 94 9.98e-02 95 9.47e-02 1e-03 200 9.83e-04 242 9.70e-04 241 9.66e-04 240 1.02e-03 1e-05 333 9.78e-06 377 9.73e-06 375 1.02e-05 377 9.94e-06 1e-07 401 9.17e-09 fail 402 6.77e-08 402 2.99e-08 1e-09 403 4.95e-10 fail 407 7.92e-10 408 8.01e-10 1e-11 412 9.18e-12 fail 430 9.22e-12 429 9.85e-12 1e-13 486 9.81e-14 fail 584 1.00e-13 583 1.04e-13 1e-15 1013 5.16e-15 fail 1024 3.31e-15 1024 2.40e-14 

Fig. 3: Matrix 3 (S-shaped spectral decay). Below its precision wall, randQB EI ’s error indicator stops decreasing (right panel) and never meets the tolerance; those entries are marked fail .

Error indicators and the breakdown of randQB EI . The methods differ in how they monitor the residual error ∥A − QB ∥F. randQB EI uses the identity ∥E∥2F =

∥A∥2F − ∥ B∥2F. While exact in infinite precision, this “top-down” subtraction is highly susceptible to catastrophic cancellation once the residual is small relative to ∥A∥F,which (as noted in [47, Theorem 3]) restricts the reliable tolerance to roughly ε ≥

2.7 × 10 −7 in double precision. Below this wall the method breaks down in one of two ways: either (i) the tracked quantity ∥A∥2F − ∥ B∥2F turns negative , so the indicator is undefined; or (ii) the indicator stops decreasing near O(√εmach ) and the stopping criterion is never met, so the iteration runs to its maximum number of steps and returns a full-rank factorization with no low-rank approximation. Which behavior occurs is driven by the random sketch rather than the spectrum: either can arise for any test matrix, and the same matrix may exhibit one or the other across runs; the figures report one representative failure each. In contrast, randQB MF Fro (and 

randQB AF Fro ) use the randomized indicator ∥Y∥F = ∥(A−QB )Ω∥F ≈ ∥ A−QB ∥F,

where Ω is a normalized Gaussian matrix (i.i.d. N (0 , 1/b ) entries) with b columns. Being a norm of a sketched residual, this indicator is inherently non-negative and free of the cancellation that plagues the energy-subtraction approach, so it reliably drives the iteration down to tolerances near machine precision. 

5.3. Comparison in the Spectral Norm. We now extend the comparison to the spectral norm, evaluating four methods: the t-SVD (the optimal, but not matrix-free, baseline), the SOTA range finder randQB HMT [22, Algorithm 4.2], and the two proposed methods randQB MF Spec and randQB AF Spec . Figures 4 to 6 report the results for the exponential, polynomial, and S-shaped spectra (Matrices 1– 21 10 -15 10 -10 10 -5 10 0                                                                        

> 020 40 60 80 123456
> 10 -10
> 10 0
> ϵt-SVD randQB HMT randQB MF Spec randQB AF Spec
> krel. err krel. err krel. err krel. err 1e-01 58.21e-02 12 7.08e-03 58.21e-02 42.02e-01 1e-03 14 9.12e-04 25 1.43e-05 17 6.47e-04 12 5.24e-03 1e-05 24 6.14e-06 33 2.93e-07 25 4.59e-06 24 1.54e-05 1e-07 33 6.83e-08 42 5.22e-09 35 8.49e-08 35 6.00e-08 1e-09 42 7.58e-10 51 2.64e-11 43 9.43e-10 41 2.85e-09 1e-11 51 8.42e-12 61 5.73e-13 53 8.96e-12 54 6.34e-12 1e-13 60 9.36e-14 72 5.38e-15 65 4.68e-14 61 1.58e-13

Fig. 4: Matrix 1 (exponential, fast spectral decay). 3), respectively. Each figure shows the computed rank versus the prescribed relative tolerance ε (left) and the per-iteration error indicator of each method at the smallest tolerance (right), together with a table of the computed rank k and the achieved (exact) relative error in the spectral norm (evaluated as described in subsection 5.1; tabulated at every other tolerance for brevity, while the panels use the full range). 

Rank overestimation and comparison with randQB HMT . A significant advantage of our method is its rank efficiency compared to the existing adaptive range finder. The existing method utilizes an error indicator that includes a safety constant C =10 p2/π . While this constant provides a rigorous probabilistic bound, it acts as a significant “amplification” factor that causes the algorithm to perceive the error as much larger than it actually is in many cases. As illustrated in the left panels of Figures 4 to 6, this pessimistic stopping cri-terion leads randQB HMT to consistently overestimate the rank required to satisfy the tolerance — often inflating it to (near-)full rank at the tightest tolerances. In con-trast, randQB MF Spec and randQB AF Spec avoid this over-determination, yielding numerical ranks that are far more compact — close to the optimal t-SVD ranks for the rapidly-decaying spectra, and well below randQB HMT ’s throughout. 

Accuracy of the spectral error indicator. A key feature of randQB MF Spec is the use of the look-ahead block norm ∥Bi∥2 as a proxy for the total residual error. As shown in the right panels of Figures 4 to 6, this indicator provides a remarkably tight estimate of the true residual error throughout the iteration process. The indicator remains reliable across many orders of magnitude. 

6. Conclusions. We have introduced a family of adaptive, matrix-free random-ized QB algorithms for high-precision low-rank approximation. Together, they address three bottlenecks of existing methods: excessive data passes, rank over-determination, and the precision wall inherent in Gramian-based error estimation. Our numerical ex-22 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN 10 -5 10 0   

> 0200 400 600 800 1000 1200 010 20 30
> 10 -6
> 10 -4
> 10 -2
> 10 0
> 10 2

ϵ t-SVD randQB HMT randQB MF Spec randQB AF Spec 

k rel. err k rel. err k rel. err k rel. err 1e-01 3 6.25e-02 32 3.92e-03 3 6.25e-02 3 6.36e-02 2e-02 7 1.56e-02 75 5.57e-04 7 1.56e-02 6 2.21e-02 5e-03 14 4.44e-03 177 1.08e-04 13 5.16e-03 13 6.27e-03 1e-03 31 9.77e-04 484 1.53e-05 41 1.11e-03 66 7.66e-04 2e-04 70 1.98e-04 928 3.48e-06 104 2.44e-04 133 1.84e-04 5e-05 141 4.96e-05 1021 2.25e-06 228 6.25e-05 321 3.58e-05 1e-05 316 9.95e-06 1024 7.93e-16 488 1.42e-05 833 4.78e-06 1e-06 1000 9.98e-07 1024 6.50e-16 1024 1.15e-15 1024 1.51e-15 

Fig. 5: Matrix 2 (polynomial, slow spectral decay). This slow decay inflates all the randomized ranks above the t-SVD optimum at the tighter tolerances. 10 -15 10 -10 10 -5 10 0   

> 0200 400 600 800 1000 1200 010 20 30
> 10 -15
> 10 -10
> 10 -5
> 10 0
> 10 5

ϵ t-SVD randQB HMT randQB MF Spec randQB AF Spec 

k rel. err k rel. err k rel. err k rel. err 1e-01 67 9.83e-02 215 3.47e-03 100 9.06e-02 98 1.19e-01 1e-03 200 9.83e-04 358 3.29e-05 236 1.13e-03 229 2.00e-03 1e-05 333 9.83e-06 403 1.45e-07 387 8.52e-06 363 2.06e-05 1e-07 401 3.49e-08 407 1.98e-09 402 2.49e-08 401 2.83e-07 1e-09 404 7.03e-10 422 4.74e-11 406 8.80e-10 405 1.79e-09 1e-11 416 8.59e-12 530 2.59e-13 429 8.86e-12 425 1.73e-11 1e-13 479 9.74e-14 1020 3.38e-15 553 1.17e-13 577 9.78e-14 1e-15 1024 1.72e-14 1024 1.97e-14 1024 1.95e-14 1024 1.90e-14 

Fig. 6: Matrix 3 (S-shaped spectral decay). 23 periments demonstrate several advantages of the proposed framework: 

• Robustness in the Frobenius Norm: While the existing method randQB EI 

fails to satisfy tolerances below O(√εmach ), our matrix-free error indicator re-liably tracks the residual error down to machine precision. 

• Efficiency in the Spectral Norm: By utilizing a look-ahead block norm indicator, we avoid the pessimistic constants found in an existing adaptive range finder, yielding significantly more compact ranks that closely match the optimal truncated SVD. 

• Reduced Data Movement: When only the orthonormal basis is required, the adjoint-free variants dispense with the backward pass over the data, roughly halving the number of operator applications while degrading the ap-proximation accuracy only slightly. Several extensions are natural: 

Tensor compression. Inserting the relative-tolerance range finder into the sequen-tially truncated HOSVD [43, 9] yields a matrix-free ST-HOSVD for the Tucker format. Standard randomized implementations estimate the per-mode truncation error with a Gramian-based indicator of the randQB EI [47] type, which is limited to tolerances of order O(√εmach ) (Remark 3.2); our indicators track the residual to machine precision, so the resulting ST-HOSVD overcomes this precision wall. 

Hierarchical matrices. The construction of hierarchical ( H-)matrices, currently under active development, is a closely related target. Randomized black-box construc-tions build the representation from matrix–vector products, either by peeling off the off-diagonal blocks level by level [27, 29, 26] or, more recently, by simultaneously com-pressing and factorizing the matrix [46]. In all of these, the numerical rank fluctuates unpredictably across the many off-diagonal blocks, so fixed-rank range finders either over-allocate memory or lose fidelity, whereas the adaptive, fixed-tolerance methods of section 3 and section 4 set the rank block-by-block to a prescribed accuracy. 

High-performance software. Because the algorithms are inherently blocked, they map naturally onto BLAS-3 kernels and modern parallel hardware, making a high-performance (GPU and distributed-memory) implementation a natural next step, building on recent HPC randomized-factorization efforts [23, 30, 11, 10, 7]. A further direction is to extend these matrix-free techniques to streaming data. 

Acknowledgments. The authors used AI-based writing assistants to help edit language and improve the readability of the manuscript. All technical content was conceived, verified, and approved by the authors, who take full responsibility for the work.           

> REFERENCES [1] M. Bebendorf ,Approximation of boundary element matrices , Numerische Mathematik, 86 (2000), pp. 565–589. [2] M. Bebendorf ,Hierarchical Matrices: A Means to Efficiently Solve Elliptic Boundary Value Problems , Springer, 2008. [3] E. K. Bjarkason ,Pass-efficient randomized algorithms for low-rank matrix approximation using any number of views , 2018, https://arxiv.org/abs/1804.07531, https://arxiv.org/ abs/1804.07531. [4] T. Bui-Thanh, O. Ghattas, J. Martin, and G. Stadler ,A computational framework for infinite-dimensional bayesian inverse problems part i: The linearized case, with application to global seismic inversion , SIAM Journal on Scientific Computing, 35 (2013), pp. A2494– A2523. [5] B. Carrel ,Randomized methods for dynamical low-rank approximation , 2024, https://arxiv. org/abs/2410.17091, https://arxiv.org/abs/2410.17091.

24 ARNEL I. SMITH, ELLY DO, AND CHAO CHEN [6] S. Chandrasekaran, P. Dewilde, M. Gu, and N. Somasunderam , On the numerical rank of the off-diagonal blocks of schur complements of discretized elliptic pdes , SIAM Journal on Matrix Analysis and Applications, 31 (2010), pp. 2261–2290. [7] Y. Chen, E. N. Epperly, J. A. Tropp, and R. J. Webber , Randomly pivoted cholesky: Practical approximation of a kernel matrix with few entry evaluations , Communications on Pure and Applied Mathematics, 78 (2025), pp. 995–1041. [8] H. Cheng, Z. Gimbutas, P.-G. Martinsson, and V. Rokhlin , On the compression of low rank matrices , SIAM Journal on Scientific Computing, 26 (2005), pp. 1389–1404. [9] L. De Lathauwer, B. De Moor, and J. Vandewalle , A multilinear singular value decom-position , SIAM journal on Matrix Analysis and Applications, 21 (2000), pp. 1253–1278. [10] Y. Dong, C. Chen, P.-G. Martinsson, and K. Pearce , Robust blockwise random pivoting: Fast and accurate adaptive interpolative decomposition , SIAM Journal on Matrix Analysis and Applications, 46 (2025), pp. 1791–1815. [11] J. A. Duersch and M. Gu , Randomized qr with column pivoting , SIAM Journal on Scientific Computing, 39 (2017), pp. C263–C291. [12] H. P. Flath, L. C. Wilcox, V. Akc ¸elik, J. Hill, B. van Bloemen Waanders, and O. Ghat-tas , Fast algorithms for bayesian uncertainty quantification in large-scale linear inverse problems based on low-rank partial hessian approximations , SIAM Journal on Scientific Computing, 33 (2011), pp. 407–432. [13] P. Ghysels, T. Mary, X. S. Li, K. Turkiyyah, and S. Soubh , Robust and accurate stopping criteria for adaptive randomized sampling in matrix-free HSS construction , SIAM Journal on Scientific Computing, 38 (2016), pp. S323–S347. [14] A. Gittens and M. Mahoney , Revisiting the nystrom method for improved large-scale machine learning , in International Conference on Machine Learning, PMLR, 2013, pp. 567–575. [15] G. H. Golub and C. F. Van Loan , Matrix Computations , The Johns Hopkins University Press, Baltimore, 4th ed., 2013. [16] G. H. Golub and C. F. Van Loan , Matrix Computations , Johns Hopkins University Press, 4th ed., 2013. [17] C. Gorman, G. Ch´ avez, P. Ghysels, T. Mary, F.-H. Rouet, and X. S. Li , Robust and accurate stopping criteria for adaptive randomized sampling in matrix-free hierarchically semiseparable construction , SIAM Journal on Scientific Computing, 41 (2019), pp. S61– S85. [18] S. Gratton and D. Titley-Peloquin , Improved bounds for small-sample estimation , SIAM Journal on Matrix Analysis and Applications, 39 (2018), pp. 922–931. [19] M. Gu , Subspace iteration randomization and singular value problems , SIAM Journal on Sci-entific Computing, 37 (2015), pp. A1139–A1173. [20] M. Gu and S. C. Eisenstat , Efficient algorithms for computing a rank-revealing QR factor-ization , SIAM Journal on Scientific Computing, 17 (1996), pp. 848–869. [21] W. Hackbusch , Hierarchical Matrices: Algorithms and Analysis , Springer, 2015. [22] N. Halko, P.-G. Martinsson, and J. A. Tropp , Finding structure with randomness: Proba-bilistic algorithms for constructing approximate matrix decompositions , SIAM review, 53 (2011), pp. 217–288. [23] N. Heavner, C. Chen, A. Gopal, and P.-G. Martinsson , Efficient algorithms for computing rank-revealing factorizations on a gpu , Numerical Linear Algebra with Applications, 30 (2023), p. e2515. [24] A. Heldring, E. Ubeda, and J. M. Rius , On the accuracy of the Adaptive Cross Approxima-tion algorithm , in XI Iberian Meeting on Computational Electromagnetics, 2016, pp. 74–75. [25] M. Hutchinson , A stochastic estimator of the trace of the influence matrix for laplacian smoothing splines , Communication in Statistics- Simulation and Computation, 18 (1989), pp. 1059–1076, https://doi.org/10.1080/03610919008812866. [26] J. Levitt and P.-G. Martinsson , Linear-complexity black-box randomized compression of rank-structured matrices , SIAM Journal on Scientific Computing, 46 (2024), pp. A1747– A1763, https://doi.org/10.1137/22m1528574, http://dx.doi.org/10.1137/22M1528574. [27] L. Lin, J. Lu, and L. Ying , Fast construction of hierarchical matrix representation from matrix-vector multiplication , Journal of Computational Physics, 230 (2011), pp. 4071– 4087. [28] Q. Liu and Y. Yu , Efficient adaptive randomized algorithms for fixed-threshold low-rank ma-trix approximation , 2025, https://arxiv.org/abs/2508.07553, https://arxiv.org/abs/2508. 07553. [29] P.-G. Martinsson , Compressing rank-structured matrices via randomized sampling , SIAM Journal on Scientific Computing, 38 (2016), pp. A2572–A2594. [30] P.-G. Martinsson, G. Quintana-Orti, and N. Heavner , randutv: A blocked randomized 25 

algorithm for computing a rank-revealing utv factorization , ACM Transactions on Mathe-matical Software (TOMS), 45 (2019), pp. 1–26. [31] P.-G. Martinsson, G. Quintana Ort ´I, N. Heavner, and R. Van De Geijn , Householder qr factorization with randomization for column pivoting (hqrrp) , SIAM Journal on Scientific Computing, 39 (2017), pp. C96–C115. [32] P. G. Martinsson and J. Tropp , Randomized numerical linear algebra: Foundations and algorithms , Acta Numerica, 29 (2020), pp. 403–572. [33] P.-G. Martinsson and S. Voronin , A randomized blocked algorithm for efficiently comput-ing rank-revealing factorizations of matrices , SIAM Journal on Scientific Computing, 38 (2016), pp. S485–S507, https://doi.org/10.1137/15M1026080. [34] M. Meier and Y. Nakatsukasa , Fast randomized numerical rank estimation for numerically low-rank matrices , Linear Algebra and its Applications, 686 (2024), pp. 1–32. [35] Y. Nakatsukasa , Fast and stable randomized low-rank matrix approximation , arXiv preprint arXiv:2009.11392, (2020). [36] C. H. Paige , The computation of eigenvalues and eigenvectors of very large sparse matrices ,Ph.D. dissertation, London University, (1971). [37] K. Pearce, C. Chen, Y. Dong, and P.-G. Martinsson , Adaptive parallelizable algorithms for interpolative decompositions via partially pivoted lu , Numerical Linear Algebra with Applications, 32 (2025), p. e70002. [38] K. J. Pearce and P.-G. Martinsson , Randomized algorithms for low-rank matrix and ten-sor decompositions , 2025, https://arxiv.org/abs/2512.05286, https://arxiv.org/abs/2512. 05286. [39] A. S. D. Perrotolo, Y. Diouane, S. G¨ urol, and X. Vasseur , A unified error analysis for randomized low-rank approximation with application to data assimilation , 2024, https: //arxiv.org/abs/2405.04811, https://arxiv.org/abs/2405.04811. [40] N. Pritchard, T. Park, Y. Nakatsukasa, and P.-G. Martinsson , Fast rank adaptive CUR via a recycled small sketch , 2025, https://arxiv.org/abs/2509.21963, https://arxiv.org/ abs/2509.21963. [41] Y. Saad , Numerical Methods for Large Eigenvalue Problems , SIAM, 2011. [42] A. K. Saibaba , Randomized subspace iteration: Analysis of canonical angles and unitarily invariant norms , SIAM Journal on Matrix Analysis and Applications, 40 (2019), pp. 23– 48. [43] N. Vannieuwenhoven, R. Vandebril, and K. Meerbergen , A new strategy for the robotic truncation of a higher-order singular value decomposition , SIAM Journal on Scientific Computing, 34 (2012), pp. A1027–A1052. [44] J. Xia , Randomized sparse direct solvers , SIAM Journal on Matrix Analysis and Applications, 34 (2013), pp. 197–227. [45] W. Xu, W. Shen, C. Liu, and Z. Jia , A novel adaptive low-rank matrix approximation method for image compression and reconstruction , 2025, https://arxiv.org/abs/2506.22713, https: //arxiv.org/abs/2506.22713. [46] A. Yesypenko and P.-G. Martinsson , Randomized strong recursive skeletonization: Si-multaneous compression and LU factorization of hierarchical matrices using matrix– vector products , Journal of Scientific Computing, 106 (2026), https://doi.org/10.1007/ s10915-025-03157-9. [47] W. Yu, Y. Gu, and Y. Li , Efficient randomized algorithms for the fixed-precision low-rank matrix approximation , SIAM Journal on Matrix Analysis and Applications, 39 (2018), pp. 1339–1359, https://doi.org/10.1137/17M1141977.