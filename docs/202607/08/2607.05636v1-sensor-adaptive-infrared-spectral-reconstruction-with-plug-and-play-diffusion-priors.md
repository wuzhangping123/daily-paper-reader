---
title: Sensor-Adaptive Infrared Spectral Reconstruction with Plug-and-Play Diffusion Priors
title_zh: 基于即插即用扩散先验的传感器自适应红外光谱重建
authors: "Alireza Siyavashi, Jon Schlipf, Sebastian Reiter, Inga Fischer, Christian Wenger, Christian Herglotz"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05636v1"
tags: ["query:hsi-denoise"]
score: 9.0
evidence: 利用扩散先验进行高光谱感知光谱重建，直接相关
tldr: "高光谱传感成本高、体积大，限制了移动应用。本文提出ΠGDM，利用扩散模型学习光谱先验，通过伪逆投影与测量光电流一致，无需配对数据。在公共数据集上训练，平均估计误差1.502%，优于多种基线，并支持传感器自适应更换。该方法为资源受限平台提供了高精度、紧凑的光谱恢复方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05636v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1876, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05636v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05636v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 490, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05636v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 923, \"height\": 294, \"label\": \"Table\"}]"
motivation: 现有高光谱仪器昂贵笨重，不适用于移动设备，需低成本小型化的光谱重建方法。
method: 使用去噪扩散概率模型学习光谱先验，交替进行逆扩散去噪和伪逆投影以匹配传感器响应。
result: "平均估计误差1.502%，超越Tikhonov、高斯、压缩感知和MLP基线，并提供不确定性估计。"
conclusion: ΠGDM实现了传感器自适应的紧凑光谱恢复，适合资源受限平台，无需重训练。
---

## 摘要
高光谱传感能够实现材料识别；然而，最先进的光谱仪成本高昂且体积庞大，限制了其在移动应用中的使用。我们通过提出一种基于伪逆引导扩散模型（ΠGDM）的窄带光电流稀疏光谱重建方法来解决这一问题。采用ΠGDM，我们使用去噪扩散概率模型（DDPM）重建光谱，该模型在大型公开光谱数据集上训练，学习真实的光谱先验，无需配对传感器测量数据。在推理时，ΠGDM交替进行逆扩散去噪步骤与伪逆投影，通过传感器的校准响应度矩阵确保与测量光电流的一致性。因此，我们的方法具有传感器适应性：当探测器阵列改变时，只需替换伪逆投影中的响应度矩阵，无需重新训练扩散模型。由此得到的计算光谱仪实现了1.502%的平均估计误差，优于Tikhonov、高斯、压缩感知和多层感知器（MLP）基线，同时通过从ΠGDM的不同随机初始化进行蒙特卡洛采样，提供校准的不确定性估计。总之，我们的方法为资源受限平台上的光谱恢复提供了一种准确、紧凑的替代方案。

## Abstract
Hyperspectral sensing enables material identification; however, state-of-the-art spectrometers are costly and bulky, which limits their use in mobile applications. We address this by proposing sparse spectrum reconstruction from narrowband photocurrents using a pseudoinverse-guided diffusion model (ΠGDM). With ΠGDM we use a denoising diffusion probabilistic model (DDPM) to reconstruct the spectrum, which is trained on a large public spectral dataset to learn realistic spectral priors, eliminating the need for paired sensor measurements. At inference, ΠGDM alternates reverse-diffusion denoising steps with pseudoinverse projection to enforce consistency with measured photocurrents via the calibrated responsivity matrices of sensors. Consequently, our method is sensor-adaptive: when detector arrays change, we simply substitute the responsivity matrix in the pseudoinverse projection without retraining of the diffusion model. The resulting computational spectrometer achieves 1.502% average estimation error, outperforming Tikhonov, Gaussian, compressive-sensing, and multilayer perceptron (MLP) baselines, while providing calibrated uncertainty estimates via Monte Carlo sampling from different random initializations of ΠGDM. Summarizing, our approach offers an accurate, compact alternative for spectral recovery on resource-constrained platforms.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：传统高光谱仪（光谱仪）成本高、体积大、笨重，难以应用于低成本和移动平台（如无人机、便携设备）。现有计算光谱重建方法要么依赖固定传感器（如Tikhonov、压缩感知）需要重新调参，要么需要大量配对传感器-光谱数据（如MLP），泛化性差。
- **研究动机**：希望实现一种**传感器自适应**的光谱重建方法：训练时只需公开光谱数据库（无需传感器数据），推理时通过替换传感器响应度矩阵即可适应不同探测器阵列，无需重新训练。
- **整体含义**：提出ΠGDM（伪逆引导扩散模型），利用去噪扩散概率模型（DDPM）学习光谱先验，结合物理前向模型的伪逆投影，实现高精度、紧凑、可迁移的计算光谱重建。

## 2. 论文提出的方法论
### 核心思想
- **解耦先验学习与传感器模型**：DDPM在大型公开光谱数据集（USGS v7、SPECCHIO、ECOSTRESS）上训练，学习真实光谱的分布；推理时，通过**伪逆引导**将测量的窄带光电流约束集成到扩散采样过程中，无需重训练。
- **交替迭代**：每个采样步先由DDPM去噪得到初步清洁光谱，再计算测量残差并用伪逆投影修正，确保与传感器响应一致。

### 关键技术细节
- **前向物理模型**：光电流 \(i = R p + \eta\)，其中 \(i \in \mathbb{R}^m\)（m=9个传感器），\(p \in \mathbb{R}^n\)（n=301个波长，1300-1600 nm），\(R\) 是响应度矩阵。
- **DDPM训练**：
  - 前向扩散：\(p_t = \sqrt{\bar{\alpha}_t} p_0 + \sqrt{1-\bar{\alpha}_t} \epsilon\)，采用余弦噪声调度（offset s=0.008）。
  - 网络：1D U-Net（4.8M参数），时间条件嵌入，4个分辨率层级，通道基数96，使用SiLU激活、组归一化、dropout 5%。
  - 损失函数：\(\mathcal{L}(\theta) = \mathbb{E}_{p_0,\epsilon,t} [\|\epsilon - \epsilon_\theta(p_t, t)\|_2^2]\)。
- **ΠGDM推理**：
  - 从纯噪声 \(p_T \sim \mathcal{N}(0,I)\) 开始。
  - 对每个步长t（共K=50步，DDIM子采样）：
    1. **去噪**：U-Net预测噪声 \(\hat{\epsilon}\)，得到清洁估计 \(\hat{p}_0\)。
    2. **残差**：\(r = i - A(\hat{p}_0)\)，其中 \(A(p) = \frac{1}{2}R p + \frac{1}{2}R 1_n\)（将DDPM归一化的[-1,1]映射到光电流）。
    3. **投影**：\(p_{0,proj} = \hat{p}_0 + \gamma A^+(r)\)，\(\gamma=1.0\)（Moore-Penrose伪逆）。
    4. **DDPM步进**：由 \(p_{0,proj}\) 计算 \(p_{t-1}\) 用于下一轮。
  - 最终得到重建光谱 \(\hat{p} \in [0,1]^n\)。
- **不确定性估计**：重复128次不同随机初始化，取点wise 5%-95%分位数作为90%置信区间。

## 3. 实验设计
- **数据集**：
  - **训练DDPM**：USGS v7（矿物光谱）、SPECCHIO（1429-2500 nm，缺失部分用USGS+ECOSTRESS均值填充）、ECOSTRESS（植被）。全部归一化到1300-1600 nm范围。
  - **测试**：两套独立校准的SiGe传感器阵列（各9通道），包含6个样本（P1-P6）以及土壤、PVC、PET等材料。
- **基准方法**：
  - Tikhonov正则化（二阶导数平滑，NNLS）
  - Gaussian基函数法（假定时基叠加，矩阵求逆）
  - 压缩感知（DCT字典+Elastic-Net，ρ=0.95，Λ网格搜索）
  - MLP先验（类似文献[11]的生成器网络）
- **评价指标**：RMSE（均方根误差）、MAPE（平均绝对百分比误差，单位%）。
- **实验场景**：主要报告6个样本的定量结果（表I），以及两个传感器阵列的定性对比（Fig.2、Fig.3）。还验证了传感器自适应：直接用第二个阵列的响应度矩阵R2替代，无需重训练。

## 4. 资源与算力
- **文中未明确说明**：未提及GPU型号、数量、训练时长等信息。网络参数量4.8M，属于轻量级模型；推理时K=50步，每步需要一次U-Net前向和一次伪逆计算，计算开销中等。

## 5. 实验数量与充分性
- **实验数量**：主要定量结果只有表I（6个样本），加上部分额外材料（土壤等）的定性展示。无消融实验（如γ值选择、步数K、不同噪声调度的影响）。未与其他最新深度学习方法（如GAN、NN）对比。
- **充分性**：实验设计相对简洁，验证了传感器自适应能力（替换R2），也提供了不确定性校准（90%区间覆盖约90%实测）。但样本量较小（仅6个光谱+少量额外材料），可能未充分反映不同场景下的泛化性能。基准方法均经过超参数调优（在验证集上），比较公平。

## 6. 论文的主要结论与发现
- **ΠGDM平均MAPE 1.502%，RMSE 2.111**，优于所有对比方法（Tikhonov 12.536%, Gaussian 11.408%, CS 1.776%, MLP 9.800%）。
- **传感器自适应**：更换响应度矩阵后仍能准确重建，无需重训练。
- **不确定性量化**：90%置信区间校准良好（真实值落在区间内）。
- **轻量级**：适用于资源受限平台。

## 7. 优点
- **传感器自适应**：训练与传感器解耦，推理时即插即用，大幅降低部署成本。
- **数据高效**：利用公开光谱库，无需采集配对传感器数据。
- **提供不确定性**：多次采样得到概率化重建，有助于决策（如低置信度区域提示需额外测量）。
- **物理一致性**：伪逆投影确保测量约束，避免纯生成模型偏离真实物理。
- **模型轻量**：4.8M参数，适合嵌入式或移动端推理。

## 8. 不足与局限
- **实验规模有限**：仅测试了6个光谱和少量材料，未在大型测试集上评估，泛化能力存疑。
- **缺乏消融研究**：未系统分析γ、K、噪声调度等超参数的影响；未比较不同扩散步数下精度-速度权衡。
- **未与最新深度方法对比**：如基于Transformer的光谱重建、其他扩散逆方法（如DPS、RedDiff）。
- **依赖校准质量**：伪逆投影要求传感器响应度矩阵精确校准，不准确时重建劣化。
- **推理速度可能较慢**：需要50步迭代，每步调用U-Net，实时性可能受限（未报告推理时间）。
- **未讨论噪声鲁棒性**：实验中假设加性高斯噪声，实际场景可能包含更复杂噪声类型。
- **应用限制**：仅覆盖1300-1600 nm红外波段，波长扩展需要重新训练或迁移学习。

（完）
