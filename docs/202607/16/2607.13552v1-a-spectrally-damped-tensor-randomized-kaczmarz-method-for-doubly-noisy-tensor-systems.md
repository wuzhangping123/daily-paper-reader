---
title: A Spectrally Damped Tensor Randomized Kaczmarz Method for Doubly Noisy Tensor Systems
title_zh: 一种用于双重噪声张量系统的谱阻尼张量随机Kaczmarz方法
authors: "Nahyun Lee, Jiyoung Choi"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13552v1"
tags: ["query:hsi-denoise"]
score: 8.0
evidence: 谱阻尼张量随机Kaczmarz方法用于含噪张量系统，与高维张量数据恢复相关
tldr: 针对张量线性系统在双噪声扰动下求解不稳定的问题，本文提出谱阻尼张量随机Kaczmarz方法。通过傅里叶域频率依赖的阻尼参数抑制噪声放大，理论证明了误差递推分离传播与注入。数值实验表明该方法在噪声和病态条件下比标准TRK更稳定，并用于图像重建展示了实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1308, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1307, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1302, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 969, \"height\": 633, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13552v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 971, \"height\": 631, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13552v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1301, \"height\": 260, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13552v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1303, \"height\": 468, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13552v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1225, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13552v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1321, \"height\": 900, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13552v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1095, \"height\": 248, \"label\": \"Table\"}]"
motivation: 标准TRK在双噪声扰动下出现噪声限制和半收敛行为，需要改进鲁棒性。
method: 提出谱阻尼张量随机Kaczmarz方法，在傅里叶域按切片进行阻尼更新，实现频变阻尼。
result: 理论给出了精确误差递推并揭示速度鲁棒权衡，数值实验验证了稳定性和图像重建效果。
conclusion: SD-TRK在噪声和病态张量系统中相比标准TRK显著提高鲁棒性和准确性。
---

## 摘要
张量随机Kaczmarz（TRK）方法是在t-乘积框架下求解张量线性系统的有效行迭代求解器。我们研究了它们在双重噪声扰动模型下的行为。在该模型中，系统张量和右手边张量均受到污染。首先，我们分析了标准TRK，并推导出一个包含两项的期望误差递归。一项是收缩的，另一项是持续扰动项。这解释了当观测到的张量系统不一致时可能出现的噪声限制和半收敛行为。然后，我们引入了一种谱阻尼张量随机Kaczmarz方法（SD-TRK）。我们证明了SD-TRK的期望误差递归，该递归将误差传播与噪声注入分离开。该界限明确指出了速度与鲁棒性之间的权衡。我们还给出了一种基于FFT的实现，该实现在傅里叶域中逐切片应用阻尼更新。这种实现在实践中允许使用频率相关的阻尼参数。在合成张量系统上的数值实验说明了在噪声和病态条件下，SD-TRK相对于标准TRK的稳定化行为。我们还在相同的噪声重建流程下进行了两次通过图像重建比较。

## Abstract
Tensor randomized Kaczmarz (TRK) methods are efficient row-action solvers for tensor linear systems under the t-product framework. We study their behavior under a doubly noisy perturbation model. In this model, both the system tensor and the right-hand side tensor are corrupted. We first analyze standard TRK and derive an expected error recursion with two terms. One term is contractive, and the other is a persistent perturbation term. This explains the noise-limited and semi-convergent behavior that can occur when the observed tensor system is inconsistent. We then introduce a spectrally damped tensor randomized Kaczmarz method (SD-TRK). We prove an expected error recursion for SD-TRK that separates error propagation from noise injection. The bound makes explicit a speed-robustness trade-off. We also give an FFT-based implementation that applies the damped update slice-wise in the Fourier domain. This implementation allows frequency-dependent damping parameters in practice. Numerical experiments on synthetic tensor systems illustrate the stabilization behavior of SD-TRK relative to standard TRK in noisy and ill-conditioned settings. We also include a two-pass image reconstruction comparison under the same noisy reconstruction pipeline.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：张量随机Kaczmarz（TRK）方法是求解t-乘积框架下张量线性系统的有效行迭代求解器。已有理论主要针对**一致系统**（无噪声）并保证了线性收敛。然而，在许多实际应用中（如多通道测量、图像重建），系统张量（系数）和右手边张量（观测）**同时被噪声污染**，导致系统不一致，标准TRK会出现噪声放大和半收敛现象，无法准确收敛到无噪声解。
- **整体含义**：本文致力于解决双重噪声张量系统的鲁棒求解问题。通过分析标准TRK在双噪声下的误差结构，提出一种结合松弛参数和Tikhonov型谱阻尼的改进方法（SD-TRK），并在傅里叶域实现频率依赖的阻尼，从而显著提升算法在噪声和病态条件下的稳定性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在标准TRK更新中引入**松弛参数ω**（控制步长）和**阻尼参数λ**（正则化归一化项），将原始投影更新改为谱阻尼更新，抑制由小奇异值或弱行切片能量引起的噪声放大。同时，利用FFT在傅里叶域逐切片更新，允许对不同频率分量施加不同强度的阻尼（频率依赖λ(k)），实现更精细的正则化。
- **关键技术细节**：
  - 标准TRK更新：\( X^{t+1} = X^t - \tilde{A}_{i_t::}^* (\tilde{A}_{i_t::} \tilde{A}_{i_t::}^*)^{-1} (\tilde{A}_{i_t::} X^t - \tilde{B}_{i_t::}) \)
  - SD-TRK更新（公式4.2）：\( X^{t+1} = X^t - \omega \tilde{A}_{i_t::}^* (\tilde{A}_{i_t::} \tilde{A}_{i_t::}^* + \lambda I)^{-1} (\tilde{A}_{i_t::} X^t - \tilde{B}_{i_t::}) \)
  - 当ω=1、λ=0时退化为标准TRK。
  - **FFT实现**（Algorithm 1）：在傅里叶域中，对每个频率切片k独立执行阻尼更新：  
    \( \hat{X}^{(k),t+1} = \hat{X}^{(k),t} - \omega (\hat{\tilde{A}}^{(k)}_{i_t:})^* (\hat{\tilde{A}}^{(k)}_{i_t:} (\hat{\tilde{A}}^{(k)}_{i_t:})^* + \lambda(k) I)^{-1} (\hat{\tilde{A}}^{(k)}_{i_t:} \hat{X}^{(k),t} - \hat{\tilde{B}}^{(k)}_{i_t:}) \)
  - 频率依赖阻尼参数设计（公式4.9）：\( \lambda(k) = \lambda_{\text{floor}} + \lambda_{\text{scale}} (\delta_A^2 + \delta_B^2) \|\hat{\tilde{A}}^{(k)}\|_F^2 / n_1 \)，匹配噪声水平与局部切片尺度。
- **理论结果**：
  - **定理3.3**：标准TRK在双噪声下的期望误差递归包含一个收缩项和一个持续扰动项，扰动项由频率不平衡常数γ放大。
  - **定理4.2**：SD-TRK的期望误差递归分离误差传播与噪声注入，明确揭示了速度–鲁棒性权衡：增大阻尼或减小松弛会降低扰动放大但可能削弱收缩保证。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **合成张量实验**：
  - **数据集**：随机生成高斯张量（独立标准正态），尺寸统一为 (n1=20, n2=10, n3=5, n4=5)。噪声为乘性高斯噪声（δA, δB控制幅度）。
  - **场景**：
    1. 无噪声（5.1.1）：验证标准TRK线性收敛。
    2. 双重噪声（5.1.2）：δA=δB∈{0, 1e-4, 1e-2}，展示半收敛。
    3. 良态双噪声（5.1.3）：δA=1e-3, δB=0.5；标准TRK vs SD-TRK（ω=0.9, λscale=5）。
    4. 病态双噪声（5.1.4）：通过几何衰减横向切片系数构造病态，δA=0.01, δB=0.5；标准TRK vs SD-TRK（ω=0.7, λscale=100）。
    5. 已知目标配对比较（5.1.5）：稀疏目标（前5行非零，其余为零）；控制相同采样序列，对比标准TRK vs SD-TRK（ω=0.8, λscale=50）。
    6. 噪声缩放比较（5.1.6）：δA=δB=δ∈{0, 1e-3, 5e-3, 1e-2, 5e-2}；比较SD-TRK最后100步最小二乘误差与阻尼参考界（ω=0.9, λscale=5）。
  - **基准/对比方法**：始终以标准TRK（无阻尼、ω=1, λ=0）为基线。
  - **评估指标**：真实重建误差 \(\|X^t - X^\star\|_F\)、观测残差 \(\|\tilde{A} X^t - \tilde{B}\|_F\)、最小二乘误差 \(\|X^t - X^{LS}\|_F\)。
- **图像重建实验（5.2）**：
  - **数据集**：UC San Diego Geisel Library RGB图像，尺寸512×339×3，像素归一化至[0,1]。退化模型：可分离高斯模糊（行/列方向，标准差8，截断半径12），加入操作噪声（δA=1e-4）和观测噪声（δB=1e-2）。
  - **流程**：两遍重建（先沿行方向，后沿列方向）。标准TRK vs SD-TRK（ω=0.3, λ=1e-2，固定标量阻尼）。
  - **评估指标**：相对误差（公式5.4）和峰值信噪比（PSNR）。

## 4. 资源与算力

- **文中明确说明**：所有实验在一台配备Apple M3 Pro芯片（12核CPU）和18GB统一内存的MacBook Pro上运行，操作系统为macOS，使用Python 3.13.3与Jupyter notebooks实现。
- **未提及**：任何GPU型号、数量或训练时长。因此，本文实验仅依赖CPU，且每次运行时间较短（合成实验约数千次迭代，图像实验约1200次迭代），属于中等规模计算。

## 5. 实验数量与充分性

- **实验数量**：
  - 合成实验共6个子章节，其中：
    - 5.1.1: 1次试验（无噪声，确认线性收敛）。
    - 5.1.2: 每噪声水平1次试验（展示半收敛）。
    - 5.1.3: 20次独立蒙特卡洛试验（良态双噪声比较）。
    - 5.1.4: 20次独立试验（病态双噪声比较）。
    - 5.1.5: 10次配对试验（控制相同采样序列）。
    - 5.1.6: 每噪声水平10次试验，共5×10=50次（噪声缩放）。
  - 图像实验：未明确重复次数，但报告了最终定量结果（表3）。
- **充分性与公平性**：
  - 合成实验覆盖了无噪声、双噪声、良态、病态、稀疏目标等典型场景；采用了多试验平均（20或10次）以降低随机性；通过配对控制变量（5.1.5）保证了方法间比较的公平性。
  - 图像实验采用相同的两遍流程和迭代次数，仅改变更新机制，对比客观。
  - 所有对比基线均为标准TRK，未与其他正则化方法（如Tikhonov直接求解、全变分等）比较，因此实验范围相对有限，但足以验证SD-TRK相对于原始TRK的改进。
  - 实验设计合理，结果可靠，但缺少与更多先进方法的竞争性对比。

## 6. 论文的主要结论与发现

- **标准TRK在双噪声下**：误差递归包含收缩项和持续扰动项，导致噪声限制和半收敛行为；扰动项被频率不平衡常数放大。
- **SD-TRK显著提升稳定性**：
  - 在良态条件下，各指标（重建误差、残差、最小二乘误差）均低于标准TRK，且残差与真实误差的收敛阶段更一致，便于监测。
  - 在病态条件下，标准TRK出现严重半收敛（误差先降后升），而SD-TRK近乎单调下降并稳定在低误差水平（最终重建误差低约5.76倍）。
  - 在稀疏目标配对实验中，SD-TRK最终误差低约3.75倍。
  - 噪声缩放实验表明最后100步最小二乘误差随噪声增加而增加，且始终低于阻尼参考界，验证了谱阻尼对噪声的抑制作用。
- **图像重建**：SD-TRK获得更低的相对误差（0.1025 vs 0.1246）和更高的PSNR（24.57 dB vs 22.88 dB），残余下降更稳定，定性重建结果更清晰（图6）。
- **理论贡献**：定理4.2的误差递归明确分离了误差传播与噪声注入，揭示了速度–鲁棒性权衡，为参数选择提供了理论依据。

## 7. 优点

- **方法论创新**：首次将谱阻尼思想引入张量Kaczmarz框架，结合松弛与Tikhonov正则化，并在傅里叶域实现频率依赖阻尼，兼顾理论简洁性与实际灵活性。
- **理论分析深刻**：不仅给出标准TRK在双噪声下的误差结构，还为新方法推导了界，清晰刻画了收缩与扰动的关系，为后续研究提供了分析模板。
- **实验设计充分且系统**：从无噪声到不同噪声水平、从良态到病态、从连续目标到稀疏目标，逐步展示方法优势；配对控制实验和噪声缩放实验增强了结论的可信度。
- **图像重建应用展示**：虽然非最先进，但证明了方法在真实任务中的可行性，且定量指标优于标准TRK基线。
- **代码与实现细节**：提供了完整的FFT算法（Algorithm 1），并详细讨论了参数设计（公式4.9），便于复现和扩展。

## 8. 不足与局限

- **理论与实现不完全匹配**：定理4.2针对标量λ推导，而实际实现（Algorithm 1）允许频率依赖λ(k)。作者虽在文本中说明这是实践扩展，但仍存在理论间隙。
- **参数选择依赖人工**：ω和λ需人工设定，文中仅通过手动调节给出固定值，未提供自适应选择策略或敏感性分析，限制了实用性。
- **对比基线单一**：仅与标准TRK对比，未与其他张量求解器（如张量Tikhonov、张量截断SVD、张量共轭梯度等）或去噪方法（如全变分、BM3D）比较，未能全面评估方法优越性。
- **图像实验较简单**：仅用单一图像、单一模糊设置，未考虑不同模糊核、噪声水平或彩色通道相关性；且所用阻尼为固定标量，未发挥频率依赖的优势（因为该例中各频率切片一致）。
- **缺少收敛性证明**：定理4.2仅给出上界递推，未证明迭代收敛到某固定点或达到最优逼近误差，也未分析渐进性态。
- **实验统计量有限**：虽有多试验平均，但未提供置信区间或标准差；图像实验未报告多次试验结果。
- **计算资源未量化**：未报告每次运行的具体时间或复杂度，不利于评估实际计算效率。

（完）
