---
title: Adaptive Randomized Pivoting for Tensor Singular Value Decomposition Model
title_zh: 张量奇异值分解模型的自适应随机主元选择
authors: "Ahmadsho Akdodshoev, Valentin Leplat, Salman Ahmadi-Asl"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26688v1"
tags: ["query:hsi-denoise"]
score: 8.0
evidence: 张量奇异值分解与CUR近似用于数据恢复
tldr: 针对张量奇异值分解模型，本文研究了如何将矩阵自适应随机枢轴化（ARP）方法扩展到t-乘积框架。提出了两种构造：ARP-T-CUR在傅里叶域对正面切片独立应用矩阵ARP，获得逐切片CUR近似并继承直接期望误差界；T-ARP为整个张量选择公共侧面和水平切片，产生真正的t-乘积张量交叉近似，但需要跨所有傅里叶切片使用相同的枢轴索引，其误差界依赖于频率对齐条件。该条件在杠杆分数分布对齐时恢复常规因子。实验表明所提方法优于标准张量交叉基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26688v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1183, \"height\": 711, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26688v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1193, \"height\": 382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26688v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1478, \"height\": 1507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26688v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1075, \"height\": 973, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26688v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 908, \"height\": 1942, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26688v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1611, \"height\": 653, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26688v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 554, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26688v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1534, \"height\": 421, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26688v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1596, \"height\": 419, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26688v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1534, \"height\": 421, \"label\": \"Table\"}]"
motivation: 将矩阵中高效的自适应随机枢轴化方法推广到张量t-乘积框架，以解决张量列子集选择问题。
method: 提出两种扩展：ARP-T-CUR在傅里叶域逐切片独立应用矩阵ARP；T-ARP选择公共侧面和水平切片，实现t-乘积意义的张量交叉近似。
result: ARP-T-CUR具有直接期望误差界；T-ARP的误差界依赖于频率对齐条件，在杠杆分数分布对齐时恢复常规因子。
conclusion: 数值实验表明公共索引张量采样方法有效，优于标准张量交叉基线。
---

## 摘要
本文研究如何将最近为矩阵列子集选择引入的自适应随机主元选择（ARP）扩展到t-乘积框架下的张量。我们提出两种构造。第一种称为ARP-T-CUR，在傅里叶域中将矩阵ARP独立应用于张量的正面切片。这给出了傅里叶切片上的CUR近似，并直接继承了矩阵理论的期望误差界。第二种构造称为T-ARP，为整个张量选择共同的侧面和水平切片。这产生了t-乘积意义上的真正张量交叉近似，但也引入了一个新的困难：必须在所有傅里叶切片上使用相同的主元索引。我们明确了这一耦合，并在频率对齐条件下证明了期望误差界，该条件衡量了共同张量级采样规则与逐切片ARP采样规则之间的差异。当杠杆分数分布在频率间对齐时，该条件恢复了通常的$r+1$型因子。我们还讨论了由此产生的张量交叉近似及其与t-DEIM的联系。在合成张量、图像和视频上的数值实验展示了所提出方法的行为，并显示了共同索引张量采样相对于标准张量交叉基线的优势。

## Abstract
This paper studies how adaptive randomized pivoting (ARP), recently introduced for matrix column subset selection, can be extended to tensors in the t-product framework. We propose two constructions. The first one, called ARP-T-CUR, applies matrix ARP independently to the frontal slices of the tensor in the Fourier domain. This gives a Fourier-slicewise CUR approximation and leads to a direct expected-error bound inherited from the matrix theory. The second construction, called T-ARP, selects common lateral and horizontal slices for the whole tensor. This produces a genuine tensor cross approximation in the t-product sense, but also introduces a new difficulty: the same pivot indices must be used across all Fourier slices. We make this coupling explicit and prove an expected-error bound under a frequency-alignment condition measuring how far the common tensor-level sampling rule is from the slice-wise ARP sampling rules. This condition recovers the usual $r+1$-type factor when the leverage-score distributions are aligned across frequencies. We also discuss the resulting tensor cross approximation and its connection with t-DEIM. Numerical experiments on synthetic tensors, images, and videos illustrate the behavior of the proposed methods and show the benefit of common-index tensor sampling over standard tensor cross baselines.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：将矩阵中高效的自适应随机主元选择（Adaptive Randomized Pivoting, ARP）方法扩展到张量t-乘积框架下，用于解决张量列子集选择问题（Tensor Column Subset Selection Problem, T-CSSP），进而实现低管秩（tubal rank）张量的有效近似。
- **背景**：张量分解（如T-SVD）是分析多维数据的核心工具，但其直接计算在大规模数据下不可行。随机化数值线性代数方法（如随机SVD、列子集选择）已成为重要替代方案。矩阵ARP方法（Cortinovis & Kressner, 2024）能以期望最优误差选取列子集，本文旨在将其推广至张量t-乘积框架，同时保持理论保证和计算效率。

### 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：两种扩展途径：
  1. **ARP-T-CUR**（傅里叶域逐切片方法）：对张量在第三维做FFT，得到一组正面切片（frontal slices），对每个切片独立应用矩阵ARP-cross算法，获得逐切片的CUR近似。优点是可直接继承矩阵理论误差界；缺点是指数在不同频率下可能不同，不能形成原始域中共同的张量骨架。
  2. **T-ARP**（共同索引张量方法）：在t-乘积意义下，为整个张量选择共同的侧面（lateral）和水平（horizontal）切片，形成真正的张量交叉近似。关键挑战在于必须所有频率共用同一索引集，这引入了**频率对齐条件**：要求张量级采样概率与每个频率的矩阵ARP采样概率之比有界。在该条件下给出期望误差界，当杠杆分数（leverage scores）在频率间完全对齐时，因子退化为经典的\(r+1\)。

- **关键技术细节**：
  - 矩阵ARP通过Householder变换稳定实现，逐次按权重采样并正交化。
  - T-ARP算法（Alg.8）：输入正交张量\(\mathcal{V}\)（\(\mathcal{V}^\top * \mathcal{V} = \mathcal{I}\)），逐次计算张量杠杆分数（\(\|\mathbf{W}^{(k-1)}(j,:,:)\|_F^2\)），归一化后采样一个索引，更新基底。
  - T-交叉近似（Alg.9）：先对右侧基底\(\mathcal{V}_R\)执行T-ARP选列，形成\(\mathcal{C}\)；对\(\mathcal{C}\)做T-QR得\(\mathcal{Q}_J\)；再对\(\mathcal{Q}_J\)执行T-ARP选行，形成交叉近似\(\mathcal{C} * \mathcal{U} * \mathcal{R}\)。
  - 理论推导：定理4.1给出ARP-T-CUR误差界\(\leq (r+1)^2\|\mathcal{X}-\mathcal{X}_r\|_F^2\)；定理4.8给出T-ARP误差界\(\leq \prod_{k=1}^r (1+\frac{\eta_k}{r-k+1})\|\mathcal{X}-\mathcal{X}*\mathcal{V}*\mathcal{V}^\top\|_F^2\)；若\(\eta_k=1\)则因子为\(r+1\)。

### 3. 实验设计

- **数据集/场景**：
  - **真实图像**：Kodak数据集（24张图，768×512×3或512×768×3），RGB，像素值归一化至[0,1]。
  - **真实视频**：YUV视频（stefan_cif.yuv，288×352×3×90帧），定性展示单个帧。
  - **合成数据**：
    - 函数张量：\(X(i,j,k)=1/(i+j+k)^{1/p}, p=2\)，尺寸60×60×60，低但非精确管秩。
    - 随机张量：\(\mathcal{Y} = \mathcal{A} * \mathcal{B}\)，\(\mathcal{A},\mathcal{B}\)为均匀随机张量，管秩固定为7，尺寸60×60×60。
- **基准（Benchmark）方法**：
  - 最优方法：截断T-SVD。
  - 不采样实际切片，但作为理论最优。
  - 张量交叉基线：均匀采样、长度平方采样、杠杆分数采样（均用T-乘积框架实现）。
  - 本文方法：ARP-T-CUR、T-ARP、T-ARP with Householder。
- **对比指标**：相对误差（Relative Error）、PSNR、SSIM。

### 4. 资源与算力

- 文中明确说明：所有实验在一台MacBook Air M3芯片、8GB RAM上完成，使用Python 3.11.14和JAX 0.9.0.1。**未使用GPU**，也未报告具体运行时间。算力需求较低，适用于个人笔记本。

### 5. 实验数量与充分性

- **实验数量**：
  - Kodak数据集：7种方法（T-SVD、均匀、长度平方、杠杆分数、ARP-T-CUR、T-ARP、T-ARP Householder），在6个不同秩（10/20/40/60/80/100）下测试，给出均值±标准差、柱状图、重建示例。表1-3分别列出相对误差、PSNR、SSIM。
  - 合成数据：两种类型（函数型、随机型）各一个张量，在秩5/10/15/20下比较6种方法（不含长度平方采样），图6展示相对误差曲线。
  - 视频数据集：仅展示一个视频的一个帧的定性结果（均匀、杠杆分数、T-ARP、原图对比），无定量指标。
- **充分性评价**：
  - **优点**：真实图像数据集全面，覆盖不同秩，统计结果稳健；合成数据验证了理论。
  - **不足**：视频实验只有定性示例，缺乏定量对比；未做消融实验（如频率对齐程度影响、不同初始化基底的对比）；仅测试了最高秩100（图像较小）；未与其他张量CUR方法（如t-DEIM、自适应交叉近似）比较。

### 6. 主要结论与发现

- ARP-T-CUR和T-ARP均可有效执行张量列子集选择，期望误差与最优T-SVD误差有理论保证的常数倍关系。
- 在共同索引的张量交叉近似中，T-ARP显著优于均匀采样和杠杆分数采样基线，尤其在高秩时PSNR和SSIM提升明显。
- 频率对齐条件在杠杆分数分布对齐时给出紧界（\(r+1\)因子），但在实际数据中可能不完全满足，导致理论因子较大，但实验表现仍好于基线。
- Householder实现的T-ARP保持了与代数版本一致的精度。
- T-ARP适用于需要基于实际切片进行近似（如存储、解释性要求）的场景，而T-SVD仍是追求最低逼近误差的首选。

### 7. 优点

- **理论贡献**：首次将矩阵ARP扩展到t-乘积张量，并引入频率对齐条件分析共同索引带来的耦合困难，推导出清晰期望误差界。
- **方法多样性**：提供两种扩展以适配不同需求（逐切片独立 vs 共同索引）。
- **实现可靠**：提供基于JAX的Python代码（作者声明是首个此类实现），且采用Householder变换保证数值稳定性。
- **实验覆盖面**：真实图像视频 + 合成数据，使用多个指标，结果统计显著。
- **开源可复现**：代码和实验结果已公开在GitHub。

### 8. 不足与局限

- **频率对齐条件不易验证**：实际中难以保证所有频率的杠杆分数分布对齐，导致理论因子可能较大，而论文未定量测量该条件在实际张量上的偏差。
- **仅处理三阶张量**：虽然声称可推广至更高阶，但未给出具体算法和实验。
- **秩固定策略**：算法需要预先指定目标管秩 \(r\)，未讨论自适应秩选择。
- **计算复杂度分析缺失**：仅提到Householder实现复杂度\(O(n_3 n_2 r^2)\)，但未与基线方法（如均匀采样、杠杆分数采样）比较运行时间，缺乏计算效率对比。
- **视频评估不足**：仅一个视频的单个帧定性展示，缺乏量化指标（PSNR/SSIM），也未对比多种方法在不同帧和秩下的性能。
- **泛化性有限**：实验图像尺寸较小（最大约500×500），对更大规模张量（如高光谱图像、4D数据）的效果未知。
- **未与其他张量CUR方法对比**：如基于t-DEIM的方法（本文引用的[15,16]），或自适应交叉近似（ACA），减弱了实验竞争力。

（完）
