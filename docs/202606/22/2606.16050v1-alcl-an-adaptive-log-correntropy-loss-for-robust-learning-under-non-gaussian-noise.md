---
title: "ALCL: An Adaptive Log-Correntropy Loss for Robust Learning under Non-Gaussian Noise"
title_zh: ALCL：一种面向非高斯噪声下鲁棒学习的自适应对数-相关熵损失
authors: "Mainak Kundu, Ria Kanjilal, Ismail Uysal"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.16050v1"
tags: ["query:hsi-denoise"]
score: 8.0
evidence: 自适应鲁棒损失用于去噪
tldr: "传统损失函数如MSE在非高斯噪声下敏感易发散，现有相关熵损失需手动固定核参数。本文提出自适应对数相关熵损失(ALCL)，通过可微重参数化与网络权重联合学习损失几何形状和尺度参数，动态抑制极端异常值。在四个含混合重尾脉冲噪声的基准数据集上，ALCL在重构保真度和分类准确率上一致优于MSE和最优调参的广义相关熵损失；高噪声下灰度图准确率提升4.75%，RGB提升4.51%。该工作提供了一种计算高效的自适应鲁棒损失，适用于非高斯环境的深度学习。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16050v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 903, \"height\": 719, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16050v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16050v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1661, \"height\": 889, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16050v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 892, \"height\": 826, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16050v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 948, \"height\": 229, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16050v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1051, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16050v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 815, \"height\": 367, \"label\": \"Table\"}]"
motivation: 传统损失函数如MSE在非高斯噪声下鲁棒性不足，已有相关熵损失依赖固定核参数，无法自适应动态噪声分布。
method: 提出ALCL，定义带可学习形状和尺度参数的对数残差模型，通过可微重参数化与网络权重联合优化，实现影响函数有界且红降。
result: "在四个含混合重尾脉冲噪声的基准数据集上，ALCL在重构和分类任务中均优于MSE及调优相关熵损失，高噪声下准确率提升达4.75%。"
conclusion: ALCL通过自适应学习损失参数，为深度学习中非高斯噪声环境提供了有效且计算高效的鲁棒损失替代方案。
---

## 摘要
在重尾和脉冲噪声下进行鲁棒深度学习仍然具有挑战性，因为传统损失函数（如均方误差MSE）对异常值表现出无界敏感性。虽然基于相关熵的目标函数提高了鲁棒性，但现有方法依赖固定的核参数，这些参数需要经验性调整且在训练过程中保持不变。为解决这些局限，我们提出了一种自适应对数-相关熵损失（ALCL），这是一种在优化过程中自适应学习其鲁棒几何形态的重尾损失函数。ALCL引入了一个对数残差模型，其形状和尺度参数通过可微重参数化与网络权重联合学习。这产生了一种原则性的最大似然公式，其影响函数具有形式有界且再下降的特性，使得损失几何能够根据不断演变的残差统计量动态调整，同时抑制极端异常值。在混合重尾和脉冲噪声下的四个广泛使用的基准数据集（涵盖灰度图像和RGB图像数据）上的对比实验表明，ALCL在重建保真度和下游分类准确率方面始终优于MSE和最优调参的广义相关熵损失。虽然在低噪声条件下性能差异较小，但在高噪声环境下，ALCL在灰度基准数据集上中位数准确率提升高达4.75%，在RGB数据集上提升4.51%，且多次运行之间的方差更小。这些结果表明，通过联合学习损失参数实现的自适应鲁棒性，为非高斯环境中基于静态相关熵损失的深度学习提供了一种计算高效的替代方案。

## Abstract
Robust deep learning under heavy-tailed and impulsive noise remains challenging because conventional losses such as mean squared error (MSE) exhibit unbounded sensitivity to outliers. Although correntropy-based objectives improve robustness, existing formulations rely on fixed kernel parameters that must be empirically tuned and remain static during training. To address these limitations, we propose an Adaptive Log-Correntropy Loss (ALCL), a heavy-tailed loss formulation that adaptively learns its robustness geometry during optimization. ALCL introduces a logarithmic residual model whose shape and scale parameters are learned jointly with network weights through differentiable reparameterization. This yields a principled maximum likelihood formulation whose influence function is formally bounded and redescending, allowing the loss geometry to adapt dynamically to evolving residual statistics while suppressing extreme outliers. Comparative experiments on four widely used benchmark datasets spanning grayscale and red-green-blue (RGB) image data under mixed heavy-tailed and impulsive noise demonstrate that ALCL consistently outperforms MSE and optimally tuned generalized correntropy losses in both reconstruction fidelity and downstream classification accuracy. While performance differences remain small under low-noise conditions, under high-noise regimes ALCL improves median accuracy by up to 4.75% on grayscale benchmarks and 4.51% on RGB datasets, with reduced variance across runs. These results demonstrate that adaptive robustness through joint learning of loss parameters provides a computationally efficient alternative to static correntropy-based losses for deep learning in non-Gaussian environments.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在医学成像、计算机视觉、自动驾驶等关键领域，深度学习模型常面临**重尾和脉冲噪声**（非高斯噪声）的干扰。传统损失函数如均方误差（MSE）基于高斯残差假设，其二次惩罚导致对异常值**无界敏感**，使模型被极端样本主导，学习到扭曲的表征。
- **现有局限**：基于信息论学习的**最大相关熵准则（MCC）** 虽能提升鲁棒性，但其核函数（如高斯核）的形状和尺度参数**固定**或仅凭经验选择，无法在训练过程中随残差分布动态调整，导致鲁棒性与实际噪声不匹配。
- **本文目标**：提出一种**自适应对数‑相关熵损失（ALCL）**，将损失函数的鲁棒几何形态（形状参数α和尺度参数σ）作为可学习参数，与网络权重通过**可微重参数化**联合优化，从而在训练中自适应地抑制极端异常值，克服静态损失函数的根本缺陷。

---

### 2. 论文提出的方法论：核心思想、关键技术细节

#### 核心思想
- 将鲁棒损失函数构建为一个 **“对数‑重尾”M‑估计器**。对于单个残差分量 \( e \)，定义：
  \[
  \mathcal{L}_{\text{ALCL}}(e; \alpha, \sigma) = \ln\left(1 + \left(\frac{|e|}{\sigma}\right)^{\alpha}\right), \quad \alpha > 1,\ \sigma > 0.
  \]
- 形状参数α控制损失曲率与尾部衰减速度（1<α<2：温和惩罚；α=2：接近二次；α>2：对中等偏差敏感但极端异常值梯度仍被抑制）；尺度参数σ归一化残差大小。

#### 关键技术细节
1. **可微重参数化**：
   - 引入两个无约束潜变量 \(\phi, \psi \in \mathbb{R}\)，通过 softplus 函数映射到定义域：
     \[
     \alpha = \text{softplus}(\phi) + 1, \quad \tilde{\sigma} = \text{softplus}(\psi), \quad \sigma = \text{clip}(\tilde{\sigma}, \sigma_{\min}, \sigma_{\max}).
     \]
   - 对σ的裁剪使用直通估计器（straight‑through estimator）以确保梯度稳定。
   - 参数 \((\phi, \psi)\) 与网络权重一同通过反向传播更新（学习率 \(\eta_\alpha, \eta_\sigma\)）。

2. **联合最大似然解释**：
   - 损失对应于一个重尾噪声模型 \( p(e|\alpha,\sigma) \propto \left(1 + (|e|/\sigma)^{\alpha}\right)^{-1} \) 的负对数似然，因此最小化 ALCL 等价于在网络参数和噪声参数上的联合最大似然估计。

3. **影响函数分析**：
   - 得分函数（影响函数）\(\psi_{\text{ALCL}}(e) = \frac{\alpha |e|^{\alpha-1}}{\sigma^\alpha + |e|^\alpha} \text{sign}(e)\)。
   - 在 \(|e| \to \infty\) 时 \(\psi \to 0\)，且全局上界为 \(\frac{1}{\sigma}(\alpha-1)^{(\alpha-1)/\alpha}<\infty\)，证明其**有界且再下降（redescending）**，保证极端异常值梯度贡献趋近于零。

4. **正则化与架构**：
   - 总体损失：\(\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{ALCL}} + \lambda_{\ell_1}\mathbb{E}[|e|] + \zeta\|W\|_2^2\)。
   - 对灰度数据使用全连接堆叠自编码器（ALCL‑SAE），对RGB数据使用卷积自编码器（ALCL‑CAE）；除最终Sigmoid外均使用LeakyReLU + BatchNorm。
   - 在RGB中采用**通道级尺度参数** \(\sigma_c\) 而非像素级，避免退化。

---

### 3. 实验设计

- **数据集**：四个基准数据集，覆盖不同模态与语义复杂度：
  - 灰度：**MNIST**（手写数字）、**Fashion‑MNIST**（服饰）—— 28×28。
  - RGB：**CIFAR‑10**（自然图像）、**SVHN**（街景门牌号）—— 32×32。

- **噪声模型**：混合重尾+脉冲噪声
  - 背景噪声：Cauchy分布（尺度参数γ控制重尾程度）。
  - 脉冲噪声：伯努利抽样选取比例P的位置，叠加幅度M的脉冲。
  - 噪声强度分为 **高噪声** 和 **低噪声** 两个水平（具体参数见表1，灰度与RGB的γ/P/M值不同，以造成相当的破坏程度）。

- **评估协议**：
  - 先训练自编码器重建去噪，然后提取**瓶颈层特征**，训练下游分类器（灰度用MLP，RGB用小型CNN）。
  - 分类准确率作为鲁棒性的量化指标。

- **对比方法**：
  - **MSE**（标准二次损失）
  - **GGCL**（广义相关熵损失，基于广义高斯核，固定形状参数α和尺度参数β，经充分调参）
  - **ALCL**（本文方法，自适应学习α和σ）

- **硬件与资源**（若文中说明）：
  - 论文**未明确提及**GPU型号、数量、训练时长或消耗的算力。仅说明使用相同架构和训练协议以确保公平比较。

---

### 4. 资源与算力

论文正文及附录中**未报告**所使用的GPU型号、数量、训练时长或其他计算资源消耗。因此无法从文中获得具体算力信息。需假设实验在标准单GPU（如NVIDIA V100或RTX 3090）上完成，但这是推断而非原文陈述。

---

### 5. 实验数量与充分性

- **实验覆盖**：
  - 4个数据集 × 2个噪声水平（高/低） × 3种损失方法 → 24种主实验配置。
  - 每个配置重复 **5次**（n=5）以报告均值±标准差，并进行配对t检验（表3）。
  - 有定性重建示例（图3）和分布箱线图（图4）。

- **消融实验**：**缺乏**针对α固定、σ固定、仅ℓ1正则等组件的消融研究。论文仅通过可视化不同α值下的影响函数（图1）和损失几何（图2）来间接展示α的作用，但未定量拆解每个模块的贡献。

- **公平性**：
  - 对所有方法使用**完全相同**的编码器‑解码器架构与训练流程。
  - GGCL的参数经过**穷举调参**以保证其最佳静态性能。
  - MSE和GGCL**未使用**权重衰减，因为文中指出权重衰减会损害其性能；而ALCL使用了ℓ2正则，这一差异可能带来轻微不公平，但作者明确解释是为稳定自适应参数学习。

- **充分性评价**：实验设计较为充分（多个数据集、噪声水平、统计检验），但缺少消融分析和更深层或端到端任务（如直接分类、目标检测）的验证，使得“泛化能力”的证明稍显不足。

---

### 6. 论文的主要结论与发现

1. **ALCL在所有场景下优于MSE和GGCL**：在高噪声下提升尤其显著，灰度数据集上中位数准确率最高提升 **4.75%**（MNIST: 88.20% vs MSE 83.45%），RGB数据集上提升 **4.51%**（SVHN: 73.66% vs MSE 71.50%）。
2. **低噪声下仍保持优势**：当噪声接近高斯时差距缩小，但ALCL仍略优且方差更小，表明鲁棒性不会以牺牲正常情况下的性能为代价。
3. **统计显著性**：t检验表明，高噪声下ALCL对所有方法大多数结果显著（p<0.05）；低噪声下对MSE也大多显著，但对GGCL部分不显著（如Fashion‑MNIST低噪声 p=0.58）。
4. **定性重建**：ALCL能有效去除脉冲伪影，同时保留边缘与颜色信息，避免过度平滑（图3）。
5. **理论支撑**：影响函数有界且再下降的数学证明，解释了自适应机制的鲁棒来源。

---

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：首次提出将**损失函数的形状参数与尺度参数**作为可学习参数，通过可微重参数化与网络权重**联合学习**，使鲁棒几何动态适应残差分布，超越传统固定核相关熵。
- **理论完整**：提供了影响函数的有界性和再下降性的严格推导，给出了自适应鲁棒性的理论保障。
- **实验设计严谨**：
  - 在四个差异较大的数据集（灰度/RGB、简单/复杂）上测试。
  - 噪声模型模拟真实世界中的混合非高斯干扰。
  - 与**网格搜索调优后的GGCL**对比，证明自适应优于最佳静态配置。
  - 使用统计检验（配对t检验）验证结果可靠性。
- **泛化性**：ALCL的计算开销几乎不增加（仅添加两个标量/向量参数），且与任何可微网络兼容。

---

### 8. 不足与局限

- **缺乏算力报告**：未提及GPU型号、训练时长等，影响可复现性评估。
- **缺少消融实验**：未单独验证α自适应 vs 固定、σ自适应 vs 固定、ℓ1正则项、权重衰减等每个组件对性能的贡献，导致“整套方案”的增益来源不够清晰。
- **应用范围有限**：仅在自编码器+下游分类的流程上验证，未测试端到端分类、检测、分割等任务；且训练数据均为小规模基准（MNIST/CIFAR-10），未评估ImageNet级别大模型下的行为和开销。
- **人工噪声模型**：使用参数化的Cauchy+脉冲噪声，真实世界噪声分布可能更复杂（如传感器噪声、对抗扰动等），需在更多真实噪声场景下验证。
- **初始化敏感性**：α和σ的初始值未被讨论或进行敏感性分析，可能影响早期训练稳定性。
- **低噪声下增益较小**：在接近高斯噪声时性能提升很有限（例如MNIST低噪声仅0.5%左右），自适应机制的优势在理想条件下不明显，可能存在计算冗余。
- **统计检验仅5次重复**：样本量较小（n=5），虽可检测较大差异，但对微小差异检验力可能不足。

---

（完）
